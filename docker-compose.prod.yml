services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    env_file: ./.env.prod
    depends_on: [postgres, minio, redis]
    restart: unless-stopped
  web:
    build: ./web
    env_file: ./.env.prod
    depends_on: [api]
    restart: unless-stopped
