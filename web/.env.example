# Required: the gw2analytics_api FastAPI gateway base URL.
# Local dev: docker compose up -d then start the gateway with
# `uv run fastapi dev apps/api/src/gw2analytics_api/main.py` and
# leave this on the default.
API_BASE_URL=http://localhost:8000
