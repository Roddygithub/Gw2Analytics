# pnpm 11+ configuration for the Next.js web app.
# The previous "pnpm.onlyBuiltDependencies" field in package.json is IGNORED
# since pnpm 11; the new home for build-script approvals is .npmrc.

# Allow sharp's native postinstall script (required by Next.js Image).
onlyBuiltDependencies[]=sharp
