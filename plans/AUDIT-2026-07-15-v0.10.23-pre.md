# Cycle-end audit — Tour 5 v0.10.23-pre (plan 045)

**Audit ID:** AUDIT-2026-07-15-v0.10.23-pre
**Cycle:** Tour 5 plan 045 — Combat readout SCAFFOLD + Workstream D-partial
**Anchor commit:** marker SHA off main on `v0.10.23-pre/mimo-half` branch tip (force-advance pattern)
**Date:** 2026-07-15

---

## §1 — Executive Summary

Tour 5 v0.10.23-pre mimo-half cycle ships the Combat-readout SCAFFOLD + Workstream D-partial in 3 atomic commits on `main`. Cycle is **hedged-rank SCAFFOLD** (the static-model floor, NO route handlers, NO aggregator dispatchers in apps/api, NO web/Frontend components). The cycle is library-only; the route-side wiring awaits a follow-up tour with `pytest` online.

Cumulative test surface: apps/api pytest 256 / web vitest 179 / web Playwright 28 = **344 tests** (all green pre-cycle; cycle ships 0 NEW test files, NO cumulative-test delta).

7 review rounds + 1 naming-NIT round clean. ruff PASS + py_compile PASS + importlib-bypass smoke PASS on synthetic event streams for the 2 NEW per-player aggregators (deterministic ordering + grand-total invariant + ASC tie-break + duration-sentinel verified end-to-end).

6 forward-blockers carried to v0.10.24+ / v0.11.0+ (Workstream D-extension + parser-stream switch + skills DB catalog + dispatcher extension + artisan route + 4 AG Grid web components).

## §2 — Deliverables

### §2.1 Backend — libs/gw2_core (1 atomic commit)

Cycle ships 4 NEW statechange event subclasses in `libs/gw2_core/src/gw2_core/models.py`:

| Event subclass | arcdps is_statechange | key field | shape |
|---|---|---|---|
| `ConditionRemoveEvent` | 71 / 72 (BuffRemove*) | condition_removal: int | mirror of BuffRemovalEvent |
| `DownEvent` | 5 (ChangeDown) | (no payload) | actor-only |
| `DeathEvent` | 4 (ChangeDead) | killed_by_agent_id + killing_skill_id Optional | actor-only + Optional forward-compat |
| `StunBreakEvent` | 56 (StunBreak) | (no payload) | actor-only |

`EventType` StrEnum grows from 5 to 9 literals. `type Event` discriminated union grows from 5 to 9 members (Discriminated union annotation: `Annotated[DamageEvent | ... | StunBreakEvent, Field(discriminator="event_type")]`). Forward-compat note on the union recommends sub-union partition beyond 12 members.

### §2.2 Backend — apps/api schemas (1 atomic commit)

Cycle ships 6 NEW wire-shape Pydantic classes in `apps/api/src/gw2analytics_api/schemas/fight.py`:

| Class | Wire-shape fields (per design doc §5.1 JSON) |
|---|---|
| `PlayerReadoutDamageOut` | dps_total/dps_power/dps_condi/strips/cc_applied/down_contribution_dps/kills |
| `PlayerReadoutHealOut` | heal_total/hps/barrier_total/barrier_ps/cleanses/stun_breaks |
| `PlayerReadoutBoonsOut` | boons_out_rate/boons_in_rate + stability/alacrity/resistance/aegis/superspeed/stealth + other_boons_out |
| `PlayerReadoutDefenseOut` | damage_taken/cc_taken/deaths/time_downed_ms/dodges/blocks/interrupts/barrier_absorbed |
| `PlayerReadoutOut` | 5 shared identity cols + 4 nested aspect blocks |
| `FightReadoutOut` | fight_id/duration_s/players |

`apps/api/src/gw2analytics_api/schemas/__init__.py` re-exports the 6 NEW with case-sensitive alphabetical ordering (ruff auto-fix accepted).

### §2.3 Backend — libs/gw2_analytics (1 atomic commit)

Cycle ships 2 NEW per-player aggregator modules in `libs/gw2_analytics/src/gw2_analytics/`:

| Module | LoC | Strict-mirror of |
|---|---:|---|
| `player_damage.py` | ~190 | `target_dps.py` (axis-flipped: `source_agent_id` instead of `target_agent_id`) |
| `player_heal.py` | ~190 | `target_healing.py` (same axis flip) |

`libs/gw2_analytics/src/gw2_analytics/__init__.py` re-exports the 4 NEW names (`PlayerDamageAggregator` / `PlayerDamageRow` / `PlayerHealAggregator` / `PlayerHealRow`) alphabetically between `PerFightTimelineRow` and `PlayerProfile`.

## §3 — Validation matrix

| Gate | Status | Tool |
|---|---|---|
| `uv run ruff check` on libs/gw2_core + libs/gw2_analytics + apps/api schemas | **GREEN** (0 violations) | ruff |
| `python -m py_compile` on the 6 NEW/MODIFIED files | **OK** | py_compile |
| Discrete-event smoke on synthetic event streams | **PASS** | importlib-bypass + Python |
| `uv run mypy` (libs/gw2_core/src + libs/gw2_analytics/src + apps/api/src) | **N/A** this cycle (operator-only) | N/A |
| `pnpm vitest run` (web) | **N/A** this cycle (operator-only; no web code changed) | N/A |
| `pnpm playwright test` (web) | **N/A** this cycle (operator-only) | N/A |
| `cd apps/api && uv run pytest apps/api/tests/` | **N/A** this cycle (operator-only; no backend test code changed) | N/A |

The 3 N/A gates are operator-only-env-blocked; the cycle is library-only so the agent-side baseline (ruff + py_compile + importlib smoke) is the strongest validation signal available offline.

## §4 — Cross-references

- [Cycle release plan](../plans/RELEASE-v0.10.23-pre.md)
- [CHANGELOG §[0.10.23-pre] entry](../CHANGELOG.md)
- [ROADMAP §1.1 v0.10.23-pre cycle shipts entry](../docs/ROADMAP.md)
- [Design doc](../docs/v0.9.0-combat-readout-design.md) (§3-7 + §9 + §11)
- [Operator wakeup workflow](../handoff.md) (§10.W2 + §17.W3)
- [Predecessor cycle (Tour 4 v0.10.22) audit](../plans/AUDIT-2026-07-15-v0.10.22.md) — Tour 4 does NOT touch the Cycle 5 surface.

## §A — Shipping invariant

> The cycle ships 0 true residuals + 6 forward-blockers (Workstream D-extension + parser-stream switch + skills DB catalog + dispatcher extension + artisan route + 4 AG Grid web components). NO PARTIAL-FIX framing. Cycle topology: 3 atomic commits on `main` per `CONTRIBUTING.md` linear-history rule.
