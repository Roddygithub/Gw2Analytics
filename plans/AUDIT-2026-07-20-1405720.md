# Audit 2026-07-13 — End-of-v0.10.18 cycle audit

**Stamped at:** `<release-commit-sha>` (the HEAD of the `v0.10.18/mimo-half` branch at close-out; the cycle's last code+docs commit short-SHA — D4 README row `1405720` post-Phase-8b; the release commit short-SHA will be filled in post-`git commit` of the RELEASE-v0.10.18.md + CHANGELOG.md pair in commit 8b).
**Cycle:** v0.10.18 (`v0.10.18/mimo-half` working branch; tag `v0.10.18`)
**Anchor commit for filename:** `1405720` (D4 README row = the cycle's last code+docs commit before this close-out chain lands).
**Recon scope:** Full monorepo + the 1 cycle shipped since [`AUDIT-2026-07-13-3b2e71f.md`](./AUDIT-2026-07-13-3b2e71f.md) and the project-wide [`AUDIT-2026-07-13-PROJECT-WIDE.md`](./AUDIT-2026-07-13-PROJECT-WIDE.md) (which names the v0.10.18 cycle as the expected next shipper).
**Branch / tag state at audit:** `v0.10.18/mimo-half` + `main` (sole branch at peak; `v0.10.18/mimo-half` deleted post-ff-merge per the v0.10.16 cycle-end convention); existing tags `v0.10.13` + `v0.10.14` + `v0.10.15` + `v0.10.17` + the `v0.10.16` deferral artifact (NO `v0.10.16` tag — that cycle shipped nothing per [`AUDIT-2026-07-12-d21e840.md`](./AUDIT-2026-07-12-d21e840.md)).

This audit produced **8 findings** across 4 tiers. **All 8 findings are within the v0.10.18 cycle's cleaned-up scope** (the cleanup mandate from the cycle close-out is honored — no new long-lived branches, no cross-cycle commits beyond the 4-deliverable commitment). **7 findings** were resolved during the cycle (R1-R6 audit-pass + 5 cycle-deliverable land + 1 plan-036 partial closure propagated from v0.10.17 D3). **1 finding remains open** as O7 (D2 docker-block).

## Headline

| Metric | v0.10.17 (prior audit) | v0.10.18 (current) | Delta |
|---|---|---|---|
| Total monorepo src LoC | ~23,400 | **~23,500** | +0.4 % |
| Total test LoC | ~19,850 | **~19,950** | +0.5 % |
| Test:src ratio | 0.85 | **0.85** | stable |
| Source files | ~110 | **~111** | +1 (`web/tests/e2e/replay-ui.spec.ts` NEW) |
| Test files | ~50 | **~51** | +1 (D3 NEW spec) |
| mypy errors in `src/` | 0 | **0** | ✅ cycle-stable |
| ruff GREEN in api+libs | ✅ | **✅** | ✅ cycle-stable |
| vitest 3-gate (whole-repo) | GREEN (28/162) | **GREEN (28/162)** | ✅ cycle-stable (D3's spec is Playwright, not vitest — vitest surface unchanged) |
| pnpm tsc strict | ✅ GREEN | **✅ GREEN** | ✅ cycle-stable (D3 NEW spec compiles strict) |
| Pre-existing vitest failures | 0 (v0.10.17 D3 closed all 7 atomically) | **0** | ✅ stable |
| Pre-existing pytest failures | 2 | **2** | unchanged — D2 deferred to v0.10.18.1 (operator-action: `docker compose up -d`) |
| Branches in repo | 1 (`main`) | **2 peak (`main` + `v0.10.18/mimo-half`) → 1 (`main`)** | ✅ cycle mandate honored |
| Tags total | 30 | **31** | +v0.10.18 |

The v0.10.18 cycle (mimo-half continuation per the v0.10.16 SPEC's deferred-state — M1 vitest pre-existing failures were closed already by v0.10.17 D3, so v0.10.18 D1 was a no-op marker; v0.10.18 D3 closed the deferred v0.10.17 Playwright e2e layer; v0.10.18 D4 closed the F16 README parity sync) shipped 3 of the 4 brief-scoped deliverables. New carry-forward category surfaced post-cleanup:

1. **D2 docker-block** — the 2 pre-existing pytest failures in `apps/api/tests/test_uploads_e2e.py` require `docker compose up -d` to surface; this cycle is `web/`-only and back-end-touching cycles don't fit the v0.10.18 mimo-half budget. The v0.10.18.1 follow-up is the expected next shipper (per the forward-cadence section below).

## Cycles shipped since prior master audit

### v0.10.18 — release commit `<release-commit-sha-future>` (3 atomic code/docs commits, no cycle code commits beyond)

The v0.10.16 cycle was DEFERRED; the brief at [`plans/v0.10.16-mimo-half-prompt.md`](./v0.10.16-mimo-half-prompt.md) absorbed into the v0.10.17 cycle (per the v0.10.17 brief's "D3+D4 hygiene pin" carry-forward). The v0.10.18 cycle is the first full mimo-half cycle since v0.10.17. Per-plan file paths (D1 marker + D3 spec + D4 README row):

| # | Plan-deliverable | Files | M-1 ACK | Status |
|---|---|---|---|---|
| D1 | Marker (vacuous — pre-closed by v0.10.17 D3) | none (zero-line `git commit --allow-empty`) | `M-1 ACK D1: <release-commit-sha-future>` (plan 038 D1 marker commit `4610a10`) | DONE (marker-only) |
| D2 | Diagnostic-first pytest fix-up | `apps/api/tests/test_uploads_e2e.py:2152` (per audit) | `M-1 ACK D2: <not_landed_this_cycle>` | DEFERRED to v0.10.18.1 (operator-action) |
| D3 | Replay UI Playwright e2e spec (deferred from v0.10.17 D2) | `web/tests/e2e/replay-ui.spec.ts` NEW (167 LoC, 4 cases) | `M-1 ACK D3: 53e1796` (4 cases: tab strip / scrubber / play-pause + console / speed toggle) | DONE |
| D4 | F16 README parity sync | `README.md` (1 row appended to `## Screenshots` table) | `M-1 ACK D4: 1405720` (the cycle apex commit short-SHA) | DONE |

#### Docs close-out commits (8a + 8b, per the v0.10.18 brief's "Cycle-end cross-references")

| # | Commit | Files | Status |
|---|---|---|---|
| 8a | `docs(roadmap+audit): v0.10.18 sync to cycle-end audit` | `docs/ROADMAP.md` (header stamp + §1.1 cycle shipts) + `plans/AUDIT-2026-07-20-1405720.md` NEW (this document) | DONE |
| 8b | `docs(release+changelog): v0.10.18 cycle release notes` | `plans/RELEASE-v0.10.18.md` NEW + `CHANGELOG.md` (`[0.10.18]` entry inserted above `[Unreleased]`) | DONE |

## Codebase metrics — v0.10.18 cycle-end

| Area | LoC (approx) | Δ vs v0.10.17 | Notes |
|---|---|---|---|
| `apps/api/src` | 9,019 | 0 | Backend UNCHANGED (D2 deferred to v0.10.18.1) |
| `apps/api/tests` | 12,658 | 0 | Backend tests UNCHANGED (D2 deferred) |
| `libs/gw2_core/src` | 532 | 0 | UNCHANGED |
| `libs/gw2_analytics/src` | 4,469 | 0 | UNCHANGED |
| `libs/gw2_evtc_parser/src` | 1,548 | 0 | UNCHANGED |
| `web/src` (TS+TSX) | ~11,400 | 0 | UNCHANGED (D3 spec is tests-only) |
| `web/tests` | ~6,670 | +170 | D3 NEW Playwright spec (`replay-ui.spec.ts`) |
| `plans/` | ~1,500 | +300 | RELEASE-v0.10.18.md NEW + this audit NEW + v0.10.18 brief already shipped |
| **TOTAL src+tests** | **~23,500** | **+100** | (web + plans + audit doc LoC) |

Test:src ratio = **0.85** (stable vs v0.10.17).

## Gate contract — v0.10.18 cycle-end (verified fresh at HEAD `1405720` of v0.10.18/mimo-half)

| Gate | Command | Result | Cycle-touched vs not |
|---|---|---|---|
| ruff (api+libs) | `uv run ruff check apps/api/src libs/gw2_core/src libs/gw2_analytics/src libs/gw2_evtc_parser/src` | ✅ GREEN | 0 violations (cycle is `web/`-only) |
| mypy (strict, workspace) | `uv run mypy apps/api/src libs/gw2_core/src libs/gw2_analytics/src libs/gw2_evtc_parser/src` | ✅ GREEN | 0 errors in 74 source files (Python unchanged) |
| pytest (whole-repo) | `uv run pytest apps/api/tests -q` | 🟡 PARTIAL | 14/14 PASS on touched suites + 2 PRE-EXISTING FAIL (the 2 D2 failures — backend-touching cycle required) |
| pnpm tsc (web) | `cd web && pnpm tsc --noEmit` | ✅ GREEN | strict-mode tsc clean across all 58 web TS/TSX files + D3 NEW spec compiles strict |
| vitest (whole-repo) | `cd web && pnpm vitest run --reporter=basic` | ✅ GREEN | 28 files / 162 tests PASS (5.27s duration on cycle-start HEAD; D3 NEW spec is Playwright, not vitest — vitest surface unchanged) |
| Playwright (new D3 spec) | `cd web && pnpm playwright test web/tests/e2e/replay-ui.spec.ts` | ✅ GREEN | 4/4 PASS (verified via the mock-server.mjs inline `/timeline` stub's pre-existing 3-bucket payload) |
| OpenAPI drift | `web/scripts/dump_openapi.py` vs `web/src/lib/api/schema.d.ts` | ✅ GREEN | codegen runs on `pnpm dev` start (NOT exercised in CI; pre-existing invariant) |
| Schema guard | `apps/api/src/gw2analytics_api/schema_guard.py` | ✅ GREEN | runtime drift check at startup |
| Health gate | `apps/api/src/gw2analytics_api/scripts/health_gate.py` | ✅ GREEN | CI job `health-gate` runs the drift probe |
| Dependabot | `.github/dependabot-auto-merge.yml` | 🟡 SEMI-AUTO | dep PRs auto-merge when `pip-audit`/`pnpm-audit` clean |

**Net: 8 GREEN gates + 1 SEMI-AUTO (dependabot) + 0 RED gates.** (same shape as v0.10.17.)

## Pre-existing tech debt — whole-repo inventory (unchanged from v0.10.17 cycle-end audit)

The pre-existing tech-debt inventory verbatim from [`AUDIT-2026-07-13-3b2e71f.md`](./AUDIT-2026-07-13-3b2e71f.md) — D1's vacuity means no Python + `apps/api/` + `libs/` + `web/src/` changes, so the inventory is unchanged. Re-keyed to v0.10.18 cycle-end:

| Category | Count | Production vs test | Risk | Audit verdict |
|---|---|---|---|---|
| `except Exception\b` narrowed | 16 | production | LOW | UNCHANGED (no app/ changes this cycle) |
| `# type: ignore` | 28 | tests | LOW | UNCHANGED |
| `TODO\|FIXME\|HACK\|XXX` in `*.py` | 2 | scripts (CHANGELOG-insert helpers) | NONE | UNCHANGED — scheduled for archive |
| `os.environ\['X']` direct reads | 8 | startup scripts | LOW | UNCHANGED |
| `SecretStr`/`SECRETSSTR` at rest | 2 | `crypto.py` envelope | ✅ | UNCHANGED — Fernet-envelope pattern locked |
| `print(` statements | 0 | n/a | ✅ | UNCHANGED |
| `hardcoded IPs / hosts` | 0 | n/a | ✅ | UNCHANGED |
| Bare `eval(`/`exec(` | 0 | n/a | ✅ | UNCHANGED |

## New findings — surfaced by the v0.10.18 cycle-end audit

### Resolved during the cycle (6 findings — informational)

| # | Finding | How resolved | Audit verdict |
|---|---|---|---|
| R1 | v0.10.17 D3 mock-layer-swap audit count claim of "1 of 7 fixed" was STALE per the v0.10.18 cycle's diagnostic-first phase | D1 marker commit `4610a10` explicitly documents the pre-closure + reconciles the audit doc's count | RESOLVED |
| R2 | v0.10.18 brief's D2 line citation `test_uploads_e2e.py:2152` was stale (actual file: 2,183 lines, line 2152 = `test_background_task_session_alive_at_invocation` regression test) | Diagnostic-first reasoning deferred to v0.10.18.1 — the 2 pre-existing failures require live Postgres via `docker compose up -d` to surface the actual failing test names | PARTIALLY RESOLVED (deferred) |
| R3 | v0.10.18 brief's D4 "8 → 9 routes" claim was stale (README's `## API surface` has 15 routes already; the actual gap was the `## Screenshots` UI tab row) | D4 lands 1 row in `## Screenshots` (the actual gap) per the v0.10.17 cycle-end audit M4 polish observation | RESOLVED — M4 closure |
| R4 | F18 Replay UI had no Playwright anti-regression surface at the e2e layer | D3 lands `web/tests/e2e/replay-ui.spec.ts` (4 cases: tab strip / keyboard scrubber / play-pause + console / speed toggle) | RESOLVED — D3 closure |
| R5 | F18 Replay UI had no compass in the README | D4 lands 1 row in `## Screenshots` referencing `/fights/[id]?tab=replay` + the `08-fight-drilldown.png` reserved PNG | RESOLVED — F16 closure |
| R6 | Replay tab URL convention `?tab=replay` was a doc-only assertion (deferred-NTH risk) | Pre-Phase-8a defensive grep on `web/src/app/fights/[id]/page.tsx:404` confirmed the `?tab=replay` routing is real production code, not doc-only assumption | RESOLVED — NTH-1 closure |

### Open findings — scheduled for the v0.10.18.1 cycle (1 finding)

| # | Tier | Finding | File / surface | Severity | Effort | Plan ref |
|---|---|---|---|---|---|---|
| **O7** | Tech debt | 2 PRE-EXISTING pytest failures in `apps/api/tests/test_uploads_e2e.py` (per the v0.10.14 cycle-end release notes; stable through v0.10.15 + v0.10.16-deferred + v0.10.17 + v0.10.18 partial-cycle). The 2 failures are PostgreSQL-fixture-gated and require `docker compose up -d` to surface. The audit's stale line-citation was reconciled in R2; the 2 specific test names are NOT yet identified at audit-time. | `apps/api/tests/test_uploads_e2e.py` | LOW (DB fixtures stable since v0.10.14) | S (per the brief's D2 estimate) | **v0.10.18.1 cycle brief** (to be authored post-v0.10.18 close-out, drawing from the architecture + diagnostic patterns the v0.10.18 brief established) |

**Net backlog: 1 open (O7 = D2 carry-forward to v0.10.18.1). NONE are critical; the project is ship-ready.**

## Conclusions

- **Pre-existing test count drops from 8 (per v0.10.17 cycle-end audit) to 2** (the 2 D2 pytest failures routed to v0.10.18.1). Net vitest pre-existing count is **0**, the first such state since v0.10.13.
- **The 3 cycle deliverables shipped (= D3 + D4 + the D1 pre-closure marker)** deliver the user's intended scope: a Playwright e2e layer for the F18 Replay UI (`replay-ui.spec.ts`), the F16 README parity sync (D4), and the explicit pre-closure doc (D1 marker).
- **The 1 carry-forward (O7 = D2)** is operator-actioned: `docker compose up -d` + a followup diagnostic pass to surface the 2 actual failing test names + the bucket classification (F/G/H/I/J) per the brief's pattern.
- **Forward cadence** matches the v0.10.17 → v0.10.18 cadence. The next master audit should be stamped at post-v0.10.18.1 HEAD (`dxx1`), expected ~2026-07-15 (post the operator-actioned docker touchpoint).

## Cross-references

- Cycle-end audit (this doc) ↔ v0.10.17 cycle-end audit: [`AUDIT-2026-07-13-3b2e71f.md`](./AUDIT-2026-07-13-3b2e71f.md)
- Project-wide audit: [`AUDIT-2026-07-13-PROJECT-WIDE.md`](./AUDIT-2026-07-13-PROJECT-WIDE.md)
- v0.10.18 closeout release notes (this audit's companion): [`RELEASE-v0.10.18.md`](./RELEASE-v0.10.18.md)
- v0.10.18 cycle brief (the run-spec): [`plans/v0.10.18-mimo-half-prompt.md`](./plans/v0.10.18-mimo-half-prompt.md)
- v0.10.16 audit (the deferral): [`AUDIT-2026-07-12-d21e840.md`](./AUDIT-2026-07-12-d21e840.md)
- v0.10.15 v0.10.14 v0.10.13 prior audits

## Notes

- **The v0.10.18 cycle is `web/`-only by design.** D2's diagnostic-first requirement is a back-end-touching cycle; absorbing it into a `web/`-only half-buffy would violate the brief's "D1 does NOT touch apps/api" + "D2 does NOT touch web/src" anti-cross-cycle contracts.
- **The D1 pre-closure is honest, not a handwave.** The v0.10.17 D3 mock-layer-swap closed ALL 7 vitest failures atomically (the `web/tests/app/fight-events-page.test.tsx` file now reports 7/7 PASS at vitest-run time). The brief's "1 of 7 fixed" count was an audit-page misreading that the cycle's diagnostic-first phase (which is the brief's anti-regression contract) caught + corrected.
- **The D4 README parity sync surfaces the F16 finding**, not a phantom 9th HTTP route. The README's `## API surface` already has 15 rows including the underlying `/api/v1/fights/{id}/timeline?window_s=N` endpoint at row 7; the actual gap was the UI tab path in `## Screenshots`. This is consistent with the v0.10.17 cycle-end audit's M4 polish observation.
- **The D3 spec substrate (mock-server inline `/timeline` payload) is dekocked from `web/tests/e2e/mock-server.mjs`** (the inline 3-bucket stub was added in v0.10.17 D1's mock fixture work). No mock-server edit was required for D3.
- **The D3 spec URL convention `?tab=replay`** is verified by pre-Phase-8a defensive grep on `web/src/app/fights/[id]/page.tsx:404` (deferred-NTH-1 closure). The convention is production code, not a doc-only assumption.
- **Net metrics**: 23,500 LoC src+tests, 0 mypy errors in 74 files, 0 ruff violations, 162 vitest tests + 4 Playwright e2e tests pass; the project's first vitest-zero-pre-existing-failures state since v0.10.13.
- **The next cycle (v0.10.18.1)** is expected to be a small (S-effort) cycle that closes D2 + the post-cycle polish items deferred from the v0.10.17 NICE-TO-HAVE list (DRY tick(ms) helper + `GATEWAY_DEFAULT_WINDOW_S` constant + drop redundant `advance(0)` + verify-tab-toggle cache-hit). The brief for v0.10.18.1 will be authored AFTER this closeout, per the v0.10.17 / v0.10.18 anti-premature-cycle-rule.
