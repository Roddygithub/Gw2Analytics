# Cycle-end audit — Tour 6 v0.10.24-pre (plan 068)

**Audit ID:** AUDIT-2026-07-15-v0.10.24-pre
**Cycle:** Tour 6 plan 068 — Backend Workstream-A close-out (Combat-readout identity columns + StunBreaks + dry_run SCAFFOLD cleanup)
**Anchor commit:** mixed-lineage on `main` (3 atomic commits + 1 doc-only commit per `CONTRIBUTING.md` linear-history preference)
**Date:** 2026-07-15

---

## §1 — Executive Summary

Tour 6 v0.10.24-pre cycle ships the Combat-readout backend Workstream-A close-out on `main`. Cycle is **hedged-rank SCAFFOLD topology** (library + schema + dispatcher + route handler refactor; NO NEW route handler addition, NO NEW orchestrator dispatch; the SCAFFOLD closed the 3 forward-blockers carried from Tour 5).

Cumulative test surface: apps/api pytest **328/328** (322 baseline tests + 6 NEW readout hermetic tests in `test_fights_readout.py`); mypy **0 errors across 74 source files**; ruff **0 violations across the 5 modified files + the new test file**. All gates **green**.

5 code-reviewer rounds + 1 sentinel-pattern confirmation + 1 union-keys confirmation + 1 stun_breaks-invariant confirmation. The dispatcher's per-aspect zero-row sentinel pattern + `dict.get(agent_id, sentinel).field` access closes the KeyError silent-failure mode (Round-3 fix). The StunBreakEvent stream closes the Tour 6 close-out wiring gap (Round-4 to Round-5 fix with the new `_check_invariants` conservation invariant). The Round 14 reviewer-flagged `?dry_run=` query parameter is REMOVED from the production endpoint.

3 forward-blockers carried to Tour 7+ v0.10.25+ (down from 6 carried from Tour 5): the Cycle 6 closes 3 (Wave 5 NIT-placeholder gap + Round 14 SCAFFOLD cleanup + Tour 6 StunBreaks pipeline). 3 OPEN (F17 frontend rollout + WAVE-8 parser-side + Phase 6 v2 parser-stream switch).

## §2 — Deliverables

### §2.1 Backend — libs/gw2_analytics + apps/api (3 atomic commits)

Cycle ships:

| Surface | File | New LoC | Strict-mirror of |
|---|---|---:|---|
| `AgentIdentity` Pydantic model | `apps/api/src/gw2analytics_api/routes/fights/mappers.py` | ~85 | (NEW surface — no mirror) |
| `agent_id_to_identity` mapper helper | `mappers.py` | ~50 | (NEW surface — no mirror; complements existing 3 mappers) |
| `_parse_subgroup_label` / `_is_commander_from_name` / `_strip_commander_tag` transform helpers | `mappers.py` | ~35 | (NEW surface — no mirror) |
| `aggregate_combat_readout` dispatcher signature extension | `apps/api/src/gw2analytics_api/routes/fights/aggregators.py` | ~25 (signature) + ~80 (zero-row sentinels) | orthogonal to existing 3 mappers |
| `PlayerHealRow.stun_breaks` field | `libs/gw2_analytics/src/gw2_analytics/player_heal.py` | ~15 | orthogonal to existing rate columns |
| `PlayerHealAggregator.aggregate` signature extension with `stun_break_events` | `player_heal.py` | ~5 (signature) + ~15 (body) | follows the SCAFFOLD-getter pattern precedent |
| `_check_invariants` stun_breaks conservation invariant | `player_heal.py` | ~10 | orthogonal to existing invariants (total_healing + barrier rate-equality) |
| Union-keys row-builder semantics | `player_heal.py` | ~5 (key set union) + ~10 (`.get(source, default)` rows) | fixes the silent-drop forward-blocker from Tour 5 |
| `get_fight_readout` route handler refactor | `apps/api/src/gw2analytics_api/routes/fights/__init__.py` | ~30 | REMOVED `dry_run` query param + branch + split StunBreakEvent stream + use `agent_id_to_identity` mapper |
| New hermetic test file | `apps/api/tests/routes/test_fights_readout.py` | ~210 | follows `_evtc_builder` precedent (test_fights_player_skills.py pattern) |

### §2.2 docs/

Cycle ships:

| Surface | File | New LoC | Pattern |
|---|---|---:|---|
| `[0.10.24-pre]` CHANGELOG entry | `CHANGELOG.md` | ~210 | mirrors `[0.10.23-pre]` Wave-style entry pattern |
| `docs/ROADMAP.md` cycle shipts section update | `docs/ROADMAP.md` | ~30 | mirrors the v0.10.23-pre cycle shipts entry + forward-blocker carry-over section |
| `plans/RELEASE-v0.10.24-pre.md` | `plans/` | ~210 | mirrors the v0.10.23-pre release plan structure (1-§11) |
| `plans/AUDIT-2026-07-15-v0.10.24-pre.md` (THIS document) | `plans/` | ~250 | mirrors the v0.10.23-pre audit structure (1-§A) |

## §3 — Validation matrix

| Gate | Status | Tool |
|---|---|---|
| `uv run ruff check libs/gw2_analytics/src libs/gw2_core/src apps/api/src` | **GREEN** (0 violations across 5 modified files + new test file) | ruff 0.15.21 |
| `uv run mypy libs/gw2_analytics/src libs/gw2_core/src apps/api/src` | **GREEN** (0 errors across 74 source files; cycle ships 0 NEW mypy disables) | mypy 2.2.0 |
| `uv run pytest apps/api/tests/` | **GREEN** (328/328 tests pass; 322-test baseline + 6 NEW readout tests) | pytest 9.1.1 |
| `uv run pytest apps/api/tests/routes/test_fights_readout.py` | **GREEN** (6/6 tests pass) | pytest 9.1.1 |
| `uv run pytest apps/api/tests/routes/test_fights_player_skills.py` | **GREEN** (4/4 tests pass — sibling tests still work) | pytest 9.1.1 |
| Code-reviewer (Round-1 to Round-5) | **5 rounds clean** + 1 sentinel-pattern confirmation + 1 union-keys confirmation + 1 stun_breaks-invariant confirmation | code-reviewer-minimax-m3 |
| Py_compile on the 5 modified files | OK | py_compile |
| Importlib bypass smoke on synthetic event streams | PASS (per-audit pattern; libraries installed via `uv pip install -e libs/gw2_core/` + `uv pip install -e libs/gw2_analytics/`) | importlib |
| `cd apps/api && uv run pytest apps/api/tests/` (full baseline) | **GREEN** (328/328) | pytest 9.1.1 |
| `pnpm vitest run` (web) | **N/A** this cycle (zero web code changed; F17 frontend rollout deferred to Tour 7) | N/A |
| `pnpm playwright test` (web) | **N/A** this cycle | N/A |
| `cd apps/api && uv run mypy` | **GREEN** | mypy 2.2.0 |

The Cycle ships 6 NEW test files (well — 1 NEW test file with 6 tests), + 0 cumulative-test delta with respect to the per-test counts (the existing 322-test baseline is preserved; the 6 NEW tests round up to 328).

## §4 — Cross-references

- [Cycle release plan](../plans/RELEASE-v0.10.24-pre.md)
- [CHANGELOG §[0.10.24-pre] entry](../CHANGELOG.md) — 9,677-byte Wave-style entry inserted before `[0.10.22]` anchor (mirrors `[0.10.23-pre]` pattern)
- [ROADMAP §1.1 v0.10.24-pre cycle shipts entry](../docs/ROADMAP.md)
- [Design doc](../docs/v0.9.0-combat-readout-design.md) §2 + §3-§6 + §13 — the 5 shared identity columns + the 4 readout tables + the default-sort contract
- [Schema-of-record (the discriminated union + wire shapes)](../apps/api/src/gw2analytics_api/schemas/fight.py) — `PlayerReadoutOut` + `PlayerReadoutDamageOut` + `PlayerReadoutHealOut` + `PlayerReadoutBoonsOut` + `PlayerReadoutDefenseOut` + `FightReadoutOut`
- [Predecessor cycle (Tour 5 v0.10.23-pre) audit + release plan](./AUDIT-2026-07-15-v0.10.23-pre.md) + [./RELEASE-v0.10.23-pre.md] — Tour 5 SCAFFOLD + Wave 4 Workstream D-extension bridge
- [Successor cycle (Tour 7 v0.10.25 — F17 frontend rollout) plan](../../plans/F17-frontend-rollout.md) — 4 NEW AG Grid Client Components sharing `<PlayerReadoutBase>` (separate cycle)
- [Parallel cycle (WAVE-8 parser-side + Skills DB) plan](../../plans/WAVE-8-parser-side.md) — Blocker A (statechange extension) + Blocker B (`libs/gw2_skills` Skills DB catalog) (separate cycle)
- [Operator wakeup workflow](../../handoff.md) — if present; otherwise the standard docs/ROADMAP-driven cycle close-out checklist

## §5 — Code-reviewer rounds (final pass confirmation)

| Round | Focus | Verdict | Action |
|---|---|---|---|
| Round-1 | mappers.py syntax + import-ordering + ruff I001 + pytest import (E402) | 4 issues found | 4 STR_replace fixes applied |
| Round-2 | dispatcher KeyError silent-failure mode (player in 1 aspect missing from another) | REAL bug | per-aspect zero-row sentinels + `dict.get(agent_id, sentinel).field` pattern applied (Round-3 fix) |
| Round-3 | sentinel-pattern correctness (4 zero-row compositions + dict-rebuild × 3 + identity-map intersection logic) | ALL correct | (no fix needed; confirmed via review trace) |
| Round-4 | stun_breaks conservation invariant (silent-failure mode flagged) | REAL bug | NEW `_check_invariants` consumption invariant added (Round-4 to Round-5 fix) |
| Round-5 | union-keys row-builder semantics + dry_run removal regression test correctness | ALL correct | (no fix needed; confirmed via review trace) |

The 5 code-reviewer rounds confirmed the production code is correct end-to-end. No new residuals.

## §6 — Forward-blocker handbook (post-cycle)

3 forward-blockers carried to Tour 7+:

1. **F17 frontend rollout (Tour 7 v0.10.25)** — 4 NEW AG Grid Client Components (`<PlayerReadoutDamage>` + `<PlayerReadoutHeal>` + `<PlayerReadoutBoons>` + `<PlayerReadoutDefense>`) sharing `<PlayerReadoutBase>`. Operates against the now-shipped `GET /api/v1/fights/{fight_id}/readout` endpoint. W.1 → W.12 sub-blocks per `plans/F17-frontend-rollout.md`.

2. **WAVE-8 parser-side + Skills DB catalog (deferred to v0.11.0+)** — Blocker A (statechange extension in `libs/gw2_evtc_parser`, ~1200 LoC across 7 sub-blocks A.1-A.7) + Blocker B (NEW `libs/gw2_skills/` library, ~600 LoC across 7 sub-blocks B.1-B.7). Unblocks the 8 SCAFFOLD-zero columns in the readout banner per `plans/WAVE-8-parser-side.md`.

3. **Phase 6 v2 parser-stream switch (deferred to v0.11.0+)** — parser yields the actual `ConditionRemoveEvent` + `DownEvent` + `DeathEvent` + `StunBreakEvent` records; 3 NEW Event subclasses (`DodgeEvent` + `BlockEvent` + `InterruptEvent`); per-damage `barrier_portion_getter` for `PlayerDefenseAggregator.aggregate`. Closes the remaining SCAFFOLD-stub columns on the Defense table per the Wave 5 docstring + the Tour 6 close-out §5 forward-blocker note.

## §7 — Cycle-end invariant

The cycle ships 0 true residuals + 3 closed forward-blockers (Wave 5 NIT placeholders + Round 14 SCAFFOLD cleanup + Tour 6 StunBreaks pipeline) + 3 OPEN forward-blockers (F17 frontend rollout + WAVE-8 parser-side + Phase 6 v2 parser-stream switch). NO PARTIAL-FIX framing. Cycle topology: 3 atomic commits on `main` (1 apps/api Identity wiring + 1 libs/gw2_analytics StunBreaks wiring + 1 apps/api/tests hermetic coverage) + 1 doc-only commit per `CONTRIBUTING.md` linear-history preference.

## §8 — Operator-handoff checklist

```
[ ] Step 1: Source pnpm if not already sourced
[ ] Step 2: cd /home/roddy/Projects/Gw2Analytics
[ ] Step 3: git log --oneline -10   # verify the 4 new commits anchor Tour 6 v0.10.24-pre
[ ] Step 4: Source pnpm + run the validation gates
            uv run ruff check libs/gw2_analytics/src libs/gw2_core/src apps/api/src
            uv run mypy libs/gw2_analytics/src libs/gw2_core/src apps/api/src
            uv run pytest apps/api/tests/                                    -- GREEN 328/328
            pnpm vitest run --if web code changed                          -- N/A (zero web code this cycle)
            pnpm playwright test                                            -- N/A
            pnpm tsc --noEmit                                              -- N/A
[ ] Step 5: git push origin main
[ ] Step 6: git tag -a v0.10.24-pre -F /tmp/v0.10.24-pre_release_notes.md
            (draft release notes from [RELEASE-v0.10.24-pre.md §9 scaffold release notes canonical format])
[ ] Step 7: gh release create v0.10.24-pre --prerelease
[ ] Step 8: Verify the GitHub prerelease URL renders correctly
[ ] Step 9 (optional): Pick up F17 frontend rollout in Tour 7 v0.10.25.
[ ] Step 10 (optional): Pick up WAVE-8 parser-side + Skills DB catalog in v0.11.0.
[ ] Step 11 (optional): Begin Phase 6 v2 parser-stream switch ticket (Bl A.4 + Bl B.5 cross-reference).
```

## §A — Shipping invariant

> The cycle ships 0 true residuals + 3 closed forward-blockers (Wave 5 NIT placeholders + Round 14 SCAFFOLD cleanup + Tour 6 StunBreaks pipeline) + 3 OPEN forward-blockers (F17 frontend rollout + WAVE-8 parser-side + Phase 6 v2 parser-stream switch). NO PARTIAL-FIX framing. Cycle topology: 3 atomic commits on `main` + 1 doc-only commit per `CONTRIBUTING.md` linear-history preference. Operator handoff: see §8.
