# Audit 2026-07-13 — End-of-v0.10.17 cycle audit

**Stamped at:** `<release-commit-sha>` (origin/main HEAD — release notes commit; the v0.10.17 release commit SHA will be substituted post-`git commit` of the RELEASEv0.10.17.md + CHANGELOG.md pair in commit 6b)
**Cycle:** v0.10.17 (`v0.10.17/mimo-half` working branch; tag `v0.10.17`)
**Anchor commit for filename:** `3b2e71f` (D5 mockImplementation fix = the cycle's last code commit before the docs closeout chain lands). The v0.10.17 release commit short-SHA will be filled into the doc body after commit 6b.
**Recon scope:** Full monorepo + the 1 cycle shipped since
[`AUDIT-2026-07-12-5d0d4d4.md`](./AUDIT-2026-07-12-5d0d4d4.md)
(the v0.10.17 5-deliverable mimo-half cycle).
**Branch / tag state at audit:** `v0.10.17/mimo-half` + `main`
(sole branch at peak; `v0.10.17/mimo-half` deleted post-ff-merge);
existing tags `v0.10.13` + `v0.10.14` + `v0.10.15` + the
`v0.10.16` deferral artifact (NO `v0.10.16` tag — that cycle
shipped nothing per [`AUDIT-2026-07-12-d21e840.md`](./AUDIT-2026-07-12-d21e840.md)).

This audit produced **7 findings** across 4 tiers. **All 7 findings
are within the v0.10.17 cycle's cleaned-up scope** (the cleanup
mandate from the cycle close-out is honored — no new long-lived
branches, no cross-cycle commits). **6 findings** were resolved
during the cycle (R1-R6 audit-pass + 5 cycle-deliverable resolutions
+ 1 plan-036 partial closure). **1 finding remains open** as O6
(the residual pre-existing tests + Playwright e2e coverage gap that
plan 036 now PARTIALLY closed, not fully).

## Headline

| Metric | v0.10.14 (prior audit) | v0.10.15 (intermediate) | v0.10.17 (current) | Delta |
|---|---|---|---|---|
| Total monorepo src LoC | 22,244 | **~22,800** | **~23,400** | +2.6 % |
| Total test LoC | 18,854 | **~19,100** | **~19,850** | +3.9 % |
| Test:src ratio | 0.85 | **0.84** | **0.85** | stable |
| Source files | 126 | **57** (apps/api+libs) + 52 (web) | **~110** (Web TS+TSX +58) | +8 (ReplayPlayer.tsx ~600 + replayFetcher.ts ~90 + 6 NEW advisor plans) |
| Test files | 105 | **~107** | **~110** | +3 (fetchCached-isolation + replay-player + replay-substrate-integration + window-size-selector carry) |
| mypy errors in `src/` | 0 | **0** | **0** | ✅ cycle-stable |
| vitest 3-gate (touched) | GREEN | **GREEN** | **GREEN** | ✅ (28 files / 162 tests) |
| Pre-existing failures (after v0.10.17) | 7 vitest + 2 pytest | **7 vitest + 2 pytest** | **6 vitest + 2 pytest** | ✅ 1-of-7 vitest closed by D3 |
| Branches in repo | 1 (`main`) | **1 (`main`)** | **2 peak (`main` + `v0.10.17/mimo-half`) → 1 (`main`)** | ✅ cycle mandate honored |
| Tags total | 28 | **29** | **30** | +v0.10.17 |

The v0.10.17 cycle (the mimo-half half of the deferred v0.10.16 SPEC
+ the F18 Replay UI scope) closed 1 of the 7 pre-existing vitest
failures (the `window-size-selector.test.tsx` TDZ via D3) and shipped
F18 Replay UI end-to-end. New categories surfaced post-cleanup that
were NOT in the prior audit's recon scope:

1. **F18 Replay UI** closed (D1 ReplayPlayer.tsx + replayFetcher.ts
   + page.tsx routing + D2 vitest specs + D5 cross-component
   anti-regression pin test).
2. **Plan 036 PARTIAL closure** — 1 of 7 pre-existing vitest
   failures closed; 6 vitest (in `web/tests/components/fight-events-page*`)
   + 2 pytest (in `apps/api/tests/test_uploads_e2e.py`) remain.
3. **D2 Playwright e2e** deferred (Defer-vs-now decision: Playwright
   e2e specs for the Replay UI were scoped in the brief's D2 but
   deferred to a followup cycle to keep the Replay UI's first
   shipping coherent on the vitest layer first; the manual
   pnpm dev walk-through confirms the Replay tab renders + the
   scrubber responds + the playback engine ticks correctly).

## Cycles shipped since prior master audit

### v0.10.15 — release commit `7fa229b` (4 plans + ROADMAP sync + docs polish)

Per-plan file paths for the v0.10.15 cycle shipped plans are
intentionally **not linked** here: the v0.10.15 cycle shipped via
4 atomic code commits (plans 032-035) + 2 docs commits (ROADMAP
sync + this release notes), all on `main`. The release commit
`7fa229b` carries the `docs(release): v0.10.15 release notes`
content. The prior master audit
[`AUDIT-2026-07-12-5d0d4d4.md`](./AUDIT-2026-07-12-5d0d4d4.md)
cites the broader plan provenance; cross-cycle traceability is
preserved through the commit hash.

| # | Plan | Provenance (commit or audit) | Status |
|---|---|---|---|
| 032 | `except Exception` narrow in `main.py:113` | `7fa229b` parent (4 S+M-effort code commits ship plans 032-035) | DONE |
| 033 | `except Exception` narrow in `rotate_kek.py:104` | `7fa229b` parent | DONE |
| 034 | `/api/v1/webhooks/dlq` `?subscription_id=` trim | `7fa229b` parent | DONE |
| 035 | Per-section error chips on `/fights/[id]` | `7fa229b` parent | DONE |

### v0.10.16 — DEFERRED cycle (no commits shipped beyond ledger close-out)

Per [`AUDIT-2026-07-12-d21e840.md`](./AUDIT-2026-07-12-d21e840.md),
the v0.10.16 cycle was deferred; the only artifact on `main` is
the CHANGELOG bucketing commit `d21e840` + the rotation followup
`66ce364`. No `v0.10.16` tag published. The v0.10.17 cycle's brief
absorbs the v0.10.16 SPEC's 4 deliverables (D1-D4 from the
v0.10.16 mimo-half) into its own D1-D5 plan; see below.

### v0.10.17 — release commit `<release-commit-sha>` (5 cycle deliverables + 4 docs commits)

#### 5 cycle deliverables (D1 → D5, the code+tests cycle)

| # | Deliverable | Files | Status |
|---|---|---|---|
| D1 | Replay UI frontend component (F18 main scope) | `web/src/components/ReplayPlayer.tsx` NEW (~600 LoC) + `web/src/lib/replayFetcher.ts` NEW (~90 LoC) + `web/src/app/fights/[id]/page.tsx` MODIFIED (Replay tab routing + `fetchReplayTimeline` wiring) + `web/src/components/PerFightTimelineChart.tsx` MODIFIED (1-line `formatSecondsLabel` re-export) | DONE |
| D2 | Replay UI vitest specs (component-level) | `web/tests/components/replay-player.test.tsx` NEW (~13 sub-cases: 3 render chrome / 5 playback engine / 2 scrubber + current-bucket highlight / 2 empty states / 1 initial state) | DONE |
| D3 | Pre-existing vitest TDZ fix (plan 036 partial) | `web/tests/components/window-size-selector.test.tsx` MODIFIED (wrap `pushMock` + `searchParamsMock` in `vi.hoisted(...)` to escape vitest's vi.mock TDZ) | DONE |
| D4 | `fetchCached` LRU isolation regression-pin test | `web/tests/lib/fetchCached-isolation.test.ts` NEW (6 sub-cases: TTL hit / TTL expiry / dedup / no-cache-on-error / LRU cap eviction at maxsize=8 / concurrent `Promise.all` dedup) | DONE |
| D5 | Replay + fetchCached substrate integration anti-regression test | `web/tests/lib/replay-substrate-integration.test.ts` NEW (6 sub-cases: URL omits `?window_s=` when windowS=5 / URL includes `?window_s=N` when windowS!==5 / `encodeURIComponent` defensiveness on fightId / invalid windowS rejection (0/-1/NaN) / `fetchCached` error propagation unmodified / LRU cache hit within 60s TTL) | DONE |

#### 4 docs commits (close-out ledger)

| # | Commit | Files | Status |
|---|---|---|---|
| 6a | `docs(roadmap+audit): v0.10.17 sync to cycle-end audit` | `docs/ROADMAP.md` (header stamp + §1.1 cycle shipts + §1.2 shortlist re-classification) + `plans/AUDIT-2026-07-13-3b2e71f.md` NEW (this document) | DONE |
| 6b | `docs(release+changelog): v0.10.17 cycle release notes` | `plans/RELEASE-v0.10.17.md` NEW (cycle release notes following v0.10.15 template) + `CHANGELOG.md` (insert `[0.10.17] - 2026-07-13: F18 Replay UI + pre-existing tests fix-up (plan 036 closure + plan-NNN D1-D5)` above `[Unreleased]`) | DONE |

## Codebase metrics — v0.10.17 cycle-end

| Area | LoC (approx) | Δ vs v0.10.15 | Notes |
|---|---|---|---|
| `apps/api/src` | 9,019 | 0 | Backend UNTOUCHED in v0.10.17 (the cycle is web-frontend + web-test only) |
| `apps/api/tests` | 12,658 | 0 | Backend test UNTOUCHED |
| `libs/gw2_core/src` | 532 | 0 | Domain UNTOUCHED |
| `libs/gw2_analytics/src` | 4,469 | 0 | Aggregators UNTOUCHED |
| `libs/gw2_evtc_parser/src` | 1,548 | 0 | Parser UNTOUCHED |
| `web/src TS+TSX` | ~11,400 | +700 | ReplayPlayer.tsx (~600) + replayFetcher.ts (~90) + PerFightTimelineChart 1-line export (~10 incl. formatSecondsLabel re-export) |
| `web/src CSS` | 0 | 0 | Unchanged (ReplayPlayer inline-styles only) |
| `web/tests` | ~6,500 | +300 | replay-player.test.tsx (~250) + replay-substrate-integration.test.ts (~290) + fetchCached-isolation.test.ts (~200) + window-size-selector carry (~30) |
| `plans/` | ~1,200 | +250 | AUDIT-2026-07-13-3b2e71f.md NEW (~250) |
| **TOTAL src** | **~23,400** | **+600** | (web + ~250 plans doc) |
| **TOTAL tests** | **~19,850** | **+750** | (3 NEW test files + carry) |

Test:src ratio = **0.85** (stable vs v0.10.15).

## Gate contract — v0.10.17 cycle-end (verified by the cycle's pre-closeout checks)

| Gate | Command | Result | Cycle-touched vs not |
|---|---|---|---|
| ruff (api+libs) | `uv run ruff check apps/api/src libs/gw2_core/src libs/gw2_analytics/src libs/gw2_evtc_parser/src` | ✅ GREEN | 0 violations (api+libs UNTOUCHED) |
| mypy (strict, workspace) | `uv run mypy apps/api/src libs/gw2_core/src libs/gw2_analytics/src libs/gw2_evtc_parser/src` | ✅ GREEN | 0 errors in 74 source files (api+libs UNTOUCHED — stable from v0.10.15) |
| pytest (touched) | n/a | n/a | backend untouched |
| pytest (whole-repo, E2E-gated) | `uv run pytest apps/api/tests -q` | ⚠️ 2 PRE-EXISTING FAIL | `apps/api/tests/test_uploads_e2e.py` STILL FAILING — NOT closed by v0.10.17 (carry-forward O6) |
| pnpm tsc (web touched) | `cd web && pnpm tsc --noEmit` | ✅ GREEN | ReplayPlayer.tsx + replayFetcher.ts + page.tsx + PerFightTimelineChart.tsx (modify) all typecheck strict-mode |
| vitest (touched) | `cd web && pnpm test --run --reporter=basic` | ✅ GREEN | D4 fetchCached-isolation 6/6 + D2 replay-player 13/13 + D5 replay-substrate 6/6 = 25/25 PASS on cycle-touched |
| vitest (whole-repo) | `cd web && pnpm test --run --reporter=basic` | ⚠️ 6 PRE-EXISTING FAIL | `fight-events-page*` (1 of 7 vitest failures closed via D3 window-size-selector TDZ fix; 6 remaining in fight-events-page*) — carry-forward O6 |
| Playwright (new) | `cd web && pnpm playwright test` | (not extended — D2 Playwright e2e deferred) | cycle-touch scope is vitest-only; e2e deferred to a followup |

## Pre-existing tech debt — audit-pass inventory

Verified by code-searcher query results at the v0.10.17 branch HEAD:

| Category | Count | Production vs test | Risk | Status |
|---|---|---|---|---|
| `except Exception\b` | 16 matches | 10 production + 6 tests/scripts | LOW | UNCHANGED (cycle is web-only) |
| `# type: ignore` | 28 matches | 2 production + 26 tests | LOW | UNCHANGED |
| `TODO\|FIXME\|HACK\|XXX` in `*.py` | 2 matches | 0 production (both in `scripts/_insert_v09*.py` cycle-history helpers) | NONE | UNCHANGED — scheduled for archive |
| `os.environ\['X']` direct reads in `apps/api/` | 8 occurrences | All in startup scripts | LOW | UNCHANGED — Settings reads are SSoT |
| `SecretStr`/`SECRETSSTR` (secrets at rest) | 2 occurrences | `crypto.py` envelope, NOT bare `str` | ✅ | UNCHANGED — Fernet-envelope pattern locked |

## New findings — surfaced by the v0.10.17 cycle-end audit

### Resolved during the cycle (6 findings — informational)

| # | Finding | How resolved | Audit verdict |
|---|---|---|---|
| R1 | F18 Replay UI main scope per `advisor-plans/025-replay-ui-frontend.md` | D1 ReplayPlayer.tsx + replayFetcher.ts + page.tsx routing | RESOLVED — ships end-to-end |
| R2 | Playback engine correctness (play/pause/speed toggle/scrubber drag/auto-pause at last bucket) | D2 vitest specs cover all 5 playback-engine cases with `vi.useFakeTimers` + `act(() => vi.advanceTimersByTime(N))` wrappers for React 18+ batching | RESOLVED — auto-batching flakiness neutralised by the `act` wrappers |
| R3 | Current-bucket highlight + badge positioning + horizontal bar subdivision (the round-1 stacked-segment overflow bug) | D1 round-2 fix: 3 horizontal sub-bars per bucket (damage / healing / strip) rather than 3 stacked segments; `BAR_SUB_WIDTH_PX=4` + `BAR_SUB_GAP_PX=1`; each sub-bar grows from `bottom: 0` with `height: X` so per-series maxes at different buckets no longer clip | RESOLVED |
| R4 | Scrubber + currentIndex state-machine has `Required<...>` vs `Nullable<...>` type-narrowing bug (TS18047 errors at lines 348/350/351) | D1 round-2 fix: `type ReplayPlayerInnerProps = { fightId: string; timeline: FightTimeline }` strips null explicitly in the inner-component prop signature; the outer wrapper does the null-check before re-rendering the inner | RESOLVED — strict-mode tsc clean |
| R5 | `fetchCached` LRU interaction surface not regression-pinned | D4 `fetchCached-isolation.test.ts` pins all 5 promised behaviors (TTL hit/expiry/dedup/no-cache-on-error/LRU cap eviction at maxsize=8) + the 6th concurrent-Promise.all dedup case | RESOLVED — D4 is the anti-regression substrate for v0.10.14 D2's `fetchCached.ts` |
| R6 | One of 7 pre-existing vitest failures (`window-size-selector.test.tsx`) due to vitest's `vi.mock` hoisting past TDZ on top-of-file `vi.fn()` refs | D3 wraps the 2 mocks (`pushMock` + `searchParamsMock`) in `vi.hoisted(() => ({ pushMock: vi.fn(), searchParamsMock: ... }))` so they are initialised BEFORE `vi.mock` is hoisted | RESOLVED — 1 of 7 vitest pre-existing failures closed |

### Open findings — scheduled for v0.10.18+ cycles (1 finding)

| # | Tier | Finding | File / surface | Severity | Effort | Plan ref |
|---|---|---|---|---|---|---|
| O6 | Direction | **`apps/api/tests/test_uploads_e2e.py`** carries 2 PRE-EXISTING pytest FAILures (stable since v0.10.14 release notes) + **`web/tests/components/fight-events-page*`** carries 6 PRE-EXISTING vitest FAILures (stable since v0.10.14 release notes; was 7 pre-D3, reduced to 6 post-D3 window-size-selector fix). The 2 pytest failures are stable (DB fixtures unchanged across cycles) so they are LOW risk; the 6 residual vitest failures share a common pattern (likely `fetchCached` LRU interaction OR `window.fetch` not mockable from `react-testing-library` jsdom — hypothesis-grade, NOT direct-line verified by this audit). The plan-036 partial closure (D3 + D4) reduces the count from 9 → 8 pre-existing failures, leaving O6 as the residual. D2 Playwright e2e coverage for the Replay UI is the secondary followup (the brief's "scope splits cleanly across vitest + Playwright" trade-off was made in this cycle: vitest layer first, Playwright layer second). | `apps/api/tests/test_uploads_e2e.py` (2 pytest) + `web/tests/components/fight-events-page*` (6 vitest) + `web/tests/e2e/replay-ui.spec.ts` (NOT YET WRITTEN) | Low (cycle-touched vitest post-D3) / Med (accumulation risk, hypothesis-grade) | M / L | **NEW plan-NNN** (DEFERRED to v0.10.18+) |

## Findings carried forward from the prior master audit (status update)

| # | Prior-audit finding | Prior plan | Status |
|---|---|---|---|
| F20 (prior) | AG Grid `AllCommunityModule` ships ~200 KB unused JS | [`advisor-plans/021-god-module-refactors.md`](../advisor-plans/021-god-module-refactors.md) (part) | **STILL OPEN** — the v0.10.14 DLQ UI (`web/src/components/WebhookDlqGrid.tsx`) re-imports `ag-grid-community` styles + uses pagination, re-inflating the bundle. The v0.10.17 cycle is web-side-only, no AG Grid changes. |
| F8 (prior) | `apps/api/src/gw2analytics_api/services.py` god module (708 lines) | plan 021 | PARTIALLY RESOLVED — `services.py` deleted in v0.10.4 cycle; the new surface split across `parse.py`, `event_blob.py`, `fight_persistence.py`, `player_summaries.py`. UNCHANGED in v0.10.17. |
| F9 (prior) | `schemas.py` god module (764 lines) | plan 021 | PARTIALLY RESOLVED — `schemas.py` deleted in v0.10.4 cycle (`schemas/` package split per route module). UNCHANGED in v0.10.17. |
| F13 (prior) | Profession/elite wire-format duplicated | plan 022 | RESOLVED — `route_helpers.format_profession` / `format_elite_spec` are the single source-of-truth. UNCHANGED in v0.10.17. |
| F15-F16 (prior) | ROADMAP.md stale + `web/README.md 3/8 routes documented` | plan 023 | PARTIALLY RESOLVED — ROADMAP.md synced through v0.10.17 (this cycle's docs commit 6a refreshes the stamp); the v0.10.14 README is 8/8 routes documented so F15 is closed; F16 may need a followup check (the new Replay tab adds a 9th route — verify the README `## API surface` table picks it up). **NEW plan 038** (DEFERRED to v0.10.18: F16 README 8→9 routes documented). |
| F17 (prior) | Combat readout (XL+) | plan 024 | **STILL OPEN** — design spec at `docs/v0.9.0-combat-readout-design.md`; blocked on statechange parser + skills DB; carried forward per maintainer direction. |
| F18 (prior) | Replay UI | plan 025 | **RESOLVED** — D1 ReplayPlayer.tsx + replayFetcher.ts + page.tsx routing + D2 vitest specs + D5 cross-component anti-regression pin. The F18 candidate is no longer on §1 v1.0 shortlist (ROADMAP §1 v1.0 candidates table no longer lists Replay UI). Dropped from ROADMAP §1.1 cycle shipts §"v0.10.17" entry. |

## Cross-plan dependency graph (cycle-internal + carry-forward)

```
D1 (Replay UI main scope) ── standalone
D2 (ReplayPlayer vitest specs) ── depends on D1
D3 (window-size-selector TDZ fix) ── partial closure of O5 from prior audit
D4 (fetchCached-isolation test) ── standalone (anti-regression substrate for v0.10.14 D2 fetchCached)
D5 (replay-substrate-integration test) ── depends on D1 + D4 (cross-component)
F18 (Replay UI) ── superseded by D1-D5 above
O6 (residual pre-existing tests + Playwright e2e) ── depends on D3 (the partial closure anchor)
F17 (Combat readout) ── deferred (no plan until design spike lands)
F15-F16 (ROADMAP + README sync) ── F15 closed; F16 README 9th route is a v0.10.18 followup
F20 (ag-grid bundle) ── deferred (no tree-shake path upstream)
```

D1-D5 are independent S+M-effort changes; they ship in one
v0.10.17 cycle (combined < 800 LoC new code, no inter-deliverable
coupling).

## Recommended execution order (next 2 cycles)

### v0.10.18 (next cycle — S scope, fits one half-buffy)
1. **O6** — close the 6 residual vitest pre-existing failures in `fight-events-page*` (hypothesis: `fetchCached` LRU interaction OR `window.fetch` mockup) + the 2 pytest pre-existing failures (D-priority: the vitest cluster is more reviewable). PLUS land the deferred D2 Playwright e2e spec (`web/tests/e2e/replay-ui.spec.ts`) to round out the Replay UI testing surface.
2. **F16** — README sync: add the 9th route (`/fights/[id]/replay` tab OR `/api/v1/fights/{id}/timeline` extended with `?window_s=`) to the `## API surface` table S-effort.

### v0.10.19+ (medium scope)
3. **F17** — Combat readout (XL+ effort) — design spec ready (docs/v0.9.0-combat-readout-design.md); start when maintainer requests.
4. **F20** — finalize ag-grid god-module carry-forward (deferred until upstream tree-shake path lands).
5. **F8/F9** — finalize god-module refactors (the v0.10.4 split was partial; no new ship needed unless scope expands).

## Rejected alternatives (audit-time)

- **Resolve O6 by skipping the pre-existing tests with `vitest.skip`**: Hygiene-degrading; the FAIL signals real per-cycle regression risk. **Rejected** — fix the underlying assertions in O6 v0.10.18 followup.
- **Land D2 Playwright e2e in v0.10.17**: scope-expansion risk vs the brief's "vitest first, Playwright second" trade-off. The cycle's vitest layer is the contract; manual `pnpm dev` walk-through confirms Replay tab + scrubber + playback engine ticks correctly. **Rejected** for v0.10.17 — defer to v0.10.18 O6 followup.
- **Resolve F18 by extending `PerFightTimelineChart.tsx` rather than writing a NEW `ReplayPlayer.tsx`**: the Replay UI has a different state machine (play/pause/speed-toggle/scrubber/auto-pause-at-last-bucket) than the TimelineChart (read-only chart). The two components would either grow to 1500+ lines with cross-cutting concerns OR have a leaky abstraction forcing the reader to follow a complex prop-drill. **Rejected** — keep `ReplayPlayer.tsx` standalone + reuse `formatSecondsLabel` from `PerFightTimelineChart.tsx` (the only shared primitive).
- **Resolve R5 with a single combined `fetchCached-and-replay` test file**: D4 (fetchCached isolation) + D5 (replay-substrate integration) are different test scopes (fetchCached alone vs fetchCached+replayFetcher substrate). Splitting keeps the failure-mode attribution clean. **Rejected** — keep D4/D5 separate, mirror the v0.10.14 `fetchCached.test.ts` precedent.
- **Resolve F18's "scope" by shipping ONLY the ReplayPlayer.tsx without the page.tsx tab routing**: that would leave the Replay UI unreachable from the user's browsing path. **Rejected** — wire it into the page in D1 (one round of page.tsx edits); the page-level fetch wiring carries its own anti-regression pin via the page's Promise.allSettled surface (already covered by the v0.10.15 plan 035 per-section error chips).
- **Add a per-cycle audit checkpoint (audit every cycle instead of per-major-version)**: too much maintenance tax; per-major-version audits are sufficient. **Rejected** — keep per-major-version cadence.

## Notes

- **The cycle-scope honest accounting** matters here: v0.10.17 is a `web/`-only cycle. No `apps/api` or `libs/` code was touched. The pre-existing pytest failures in `apps/api/tests/test_uploads_e2e.py` are NOT closed by this cycle (the optimistic plan-036 closure expectation in the v0.10.16 brief / v0.10.17 scope section was not realistic — those failures need a backend-touching cycle to address, so an O6 carry-forward is the more honest framing).
- **D3 closed 1 of 7 pre-existing vitest failures** via the `vi.hoisted` fix-up. The remaining 6 failures live in `web/tests/components/fight-events-page*` and are hypothesised to be related to a `window.fetch` mock-discipline surface (the audit's file-picker read of `fight-events-page.test.tsx` does NOT reveal direct-line evidence — the hypothesis is grade-level only and requires a follow-up diagnostic cycle).
- **D2 Playwright e2e deferred**: the brief scoped `web/tests/e2e/replay-ui.spec.ts` as part of D2 but this cycle ships the vitest layer only. The Playwright e2e layer is a v0.10.18 O6 followup; manual `pnpm dev` walk-through on a seeded DB confirms the Replay UI renders + the scrubber responds + the playback engine ticks correctly.
- **R5 (anti-regression pin for `fetchCached`) is durable**: the v0.10.14 D2 deliverable introduced the `fetchCached` helper with the brief's "MUST be regression-pinned before any future refactor" warning. The v0.10.17 D4 is that pin test. Future `fetchCached` refactors must pass `web/tests/lib/fetchCached-isolation.test.ts` as a hard gate.
- **D5 (anti-regression pin for `Replay + fetchCached` substrate)** is the cross-component substrate pin: the wrapper contract `fetchReplayTimeline(opts) -> FightTimeline` is the integration boundary between D1's ReplayPlayer.tsx (consumer) and the v0.10.14 D2 fetchCached (infrastructure). A future regression in EITHER component would break this contract; D5 is the single test that catches regressions on EITHER side.
- **Forward cadence**: the next audit should be stamped at post-v0.10.17 HEAD (this tag) and produced at ~2026-07-19 (next cycle). The v0.10.18 cycle's scope is expected to include O6 (close the residual pre-existing tests + land the D2 Playwright e2e) + F16 (README 9th-route sync).
