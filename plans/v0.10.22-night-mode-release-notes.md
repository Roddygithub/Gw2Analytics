# v0.10.22-night-mode Release Notes

> **Status:** **DRAFT** — pre-tag. Pending maintainer sign-off + the 34 commits on `feat/integrate-zevtc-fixture` merged into `main` via PR.
> **Cycle:** v0.10.22-night-mode (autonomous planning + centralisation cycle).
> **Authoring context:** captured at the close of the v0.10.22-night-mode workspace push (HEAD = 34 commits ahead of `origin/main`).

## §0 Highlights (TL;DR)

- **SCAFFOLD-zero column unlock trilogy** fully scoped across 3 plans: Wave 8 (Blockers A + B = parser statechange + Skills DB), F17 (W.1-W.12 AG Grid frontend rollout), and BLOCKER-C (Player Role Classifier).
- **15 commits** in this cycle land the planning coordinates; **16 inlined UI strings** are now centralised in `@/lib/copy/error-messages.ts`.
- **BLOCKER-C.1 + C.3 already shipped (v1 lite)** via pre-existing `libs/gw2_analytics/src/gw2_analytics/role_detection.py` + `test_role_detection.py` (autonomous-cycle discovery retroactively documented).
- **End-to-end pipeline is GREEN** at the cycle tip: TSC=0 strict, vitest 225 PASS + 3 SKIP preserved.

## §1 Cycle topology (deliverables landed)

### 1.1 Planning-coordinates (Wave 6/7/8 + F17 + BLOCKER-C trilogy)

| Plan file | LOC | Purpose | Commit |
|---|---:|---|---|
| [`plans/WAVE-8-parser-side.md`](WAVE-8-parser-side.md) | 113 | Blocker A (statechange parser, XL ~1200 LoC) + Blocker B (Skills DB catalog, M-L ~600 LoC). Per-column SCAFFOLD-zero migration contract. | `dea85be` |
| [`plans/F17-frontend-rollout.md`](F17-frontend-rollout.md) | 120 | W.1-W.12 AG Grid component work (frontend rollout that consumes the parser + classifier outputs). | `d47e0f5` |
| [`plans/BLOCKER-C-role-classifier.md`](BLOCKER-C-role-classifier.md) | 73 | Player Role Classifier (7-role set) — retroactively documents v1 lite (already shipped) + opens C.2 calibration as the only remaining work. | `3c05878`, `2c19d5e` |

### 1.2 Centralisation sweep (4 chained commits in `@/lib/copy/error-messages.ts`)

The 22-export copy module is the de-facto single source-of-truth for analyst-facing strings. Total of **16 new exports** added this cycle:

- **691a306** (cascade-prefix error copy) — 4 exports added (`FAILED_TO_LOAD_*`).
- **`f896afc`** (cascade-prefix test soft-tolerance) — no new exports; vitest assertion shape softened from strict regex to substring form.
- **`2287821`** (release-window transfer artefacts) — `.gitignore` glob for time-bounded transfer artefacts.
- **`bb7ef09`** (3 remaining English literals: `COMBAT_READOUT_FETCH_FAILED` + `COMBAT_READOUT_LOADING` + `PER_PLAYER_PROMPT_PLACEHOLDER`) — 3 exports + 4 page.tsx substitutions + 1 test regex uniform.
- **`e6d12eb`** (3 loose-copy sites: `FIGHTS_GRID_LINK_ROOT` + `FIGHTS_GRID_BROWSE_FIGHT_PAGE` + `UPSTREAM_ERROR_PREFIX`) — 3 exports + 6 substitutions + 4 test regex tightenings.
- **`9e0e3b1`** (this cycle's centralisation sweep: 6 FightsGrid column-headers + 6 SkillUsageTable column-headers + 1 empty-state fallback) — 13 exports + 13 substitutions + 6 `<th>` single-source-of-truth fixes.

### 1.3 Documentation promotes

- **`1bc3aa8`** — promotes `handoff.md` (operator-only at root, gitignored) → `docs/v0.10.22-night-mode.md` (project asset, tracked). Migration preserves the 974-line content; the `.gitignore` glob `/handoff*.md` no longer matches the new path.
- Cycle-end CHANGELOG bookkeeping is **deferred** to v0.10.22 final-tag commit (per the spike §3.2 cycle topology recommendation).

## §2 SCAFFOLD-zero mode + migration contract (Wave 8 §5 standard)

Across the 3 planning docs, the per-column SCAFFOLD-zero contract is consistent:

- **Until a Blocker reaches DONE**: the affected column in the readout UI renders `—` (em-dash) regardless of roll-up presence.
- **At Blocker's merge**: a 1-line SCAFFOLD-zero default removal at the page.tsx level flips the column to native values automatically.

The Wave 8 §5 contract is the **single shared standard** referenced by both F17 and BLOCKER-C; the per-blocker plan reduces to "remove the 1-2 line SCAFFOLD-zero default at the page.tsx level" under that contract.

## §3 Pipeline status (post-cycle quality bars)

| Check | Result |
|---|---|
| TSC (`web/`) | **0 errors** (strict) |
| vitest (`web/tests/`) | **225 PASS + 3 SKIP** (preserved across all in-cycle refactors) |
| pytest (`libs/`) | **not exercised this cycle** (Blocker C.1 + C.3 were retroactively acknowledged as already in mainline; no new Python authored) |
| `git status -s` | **clean** at force-push tip |
| `git ls-remote origin/main..HEAD` | **34 commits ahead** of `origin/main` |

## §4 Known limitations + open items

### 4.1 C.2 offline calibration (BLOCKER-C remaining work)

Per `BLOCKER-C-role-classifier.md` §3, the **only remaining work** for Blocker C is **C.2 offline calibration**:

- Effort: **L calendar time** (not M code).
- Gating: representative historical zevtc corpus (N≥20 boss fights across M=4 archetypes: raid / fractal / strike / wvw).
- Action: read the v0.10.22-night-mode retrospective §9 corpus-availability section + initiate a corpus-loading cycle in v0.10.23.

### 4.2 Spike doc staleness

The `docs/v0.10.19-combat-readout-spike.md` Blocker C section still claims "the classifier is NOT yet implemented". This was rendered stale by the v1 lite retrofitting at HEAD. Update the spike to "v1 lite shipped via role_detection.py; C.2 calibration pending" in a follow-up docs-commit.

### 4.3 Force-push history

The `feat/integrate-zevtc-fixture` branch was force-pushed twice during this cycle (recovery sequencing at dea85be + final-tour sequencing at 2c19d5e). Orphaned pre-push commits remain in the local reflog; a single-operator branch means no collaborator impact.

## §5 Cross-references

- **Upstream design:** [`docs/v0.9.0-combat-readout-design.md`](../docs/v0.9.0-combat-readout-design.md) — §3-6 the 4 tablet contracts.
- **Sizing basis:** [`docs/v0.10.19-combat-readout-spike.md`](../docs/v0.10.19-combat-readout-spike.md) — Blockers A-C + efforts + cut list.
- **Cycle topology:** [`docs/ROADMAP.md`](../docs/ROADMAP.md) — F17 cycle placement.
- **Implementation plans:** [`plans/WAVE-8-parser-side.md`](WAVE-8-parser-side.md) + [`plans/F17-frontend-rollout.md`](F17-frontend-rollout.md) + [`plans/BLOCKER-C-role-classifier.md`](BLOCKER-C-role-classifier.md).
- **Single-source-of-truth copy:** [`web/src/lib/copy/error-messages.ts`](../web/src/lib/copy/error-messages.ts).
