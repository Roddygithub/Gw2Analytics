# v0.10.18 cycle — MiMo-half prompt

**Cycle:** v0.10.18
**Scope:** Continued pre-existing test fix-up + F16 README sync per the v0.10.17 cycle-end audit at [`plans/AUDIT-2026-07-13-3b2e71f.md`](./AUDIT-2026-07-13-3b2e71f.md) "Recommended execution order (next 2 cycles)" section:
1. **O6 — residual 6 vitest failures in `fight-events-page*`** (diagnostic-first cycle per the v0.10.17 AUDIT O6 hypothesis: `fetchCached` LRU interaction OR `window.fetch` not mockable from `react-testing-library` jsdom)
2. **O6.2 — residual 2 pytest failures in `test_uploads_e2e.py`** (stable since v0.10.14 release notes; backend-touching cycle needed)
3. **O6.3 — deferred D2 Playwright e2e spec (`web/tests/e2e/replay-ui.spec.ts`)** (`web/`-only; the v0.10.17 cycle ships vitest layer only, this cycle round out the Replay UI testing surface)
4. **F16 — README `## API surface` table 9th-route sync** (`/fights/[id]/replay` tab path + `fetchReplayTimeline` wrapper-derived read)

**Predecessor cycles:**
- v0.10.17 (`021b2b73183ab3d841de74d7fba7461d1d36f4d7`) shipped F18 Replay UI end-to-end + 1 of 7 vi closing via D3 + the deferred v0.10.16 D4 `fetchCached` LRU isolation pin + the cross-component substrate pin via D5. Tag `v0.10.17` published at <https://github.com/Roddygithub/Gw2Analytics/releases/tag/v0.10.17> (release-page publish pending GitHub Releases API recovery from transient HTTP 500).
- v0.10.16 was DEFERRED — no commits shipped beyond the CHANGELOG bucketing commit `d21e840`. The `v0.10.16/mimo-half` working branch was deleted post-handoff (commit `1b9b32c`).
- v0.10.15 (`7fa229b`) shipped plans 032-035 (audit close-out: 4 except-narrow + per-section error chips + ROADMAP sync).
- v0.10.14 (`5d0d4d4`) shipped the 4 MiMo deliverables (BFF e2e + `fetchCached` + VR baseline + ARQ CI gate) — the `fetchCached` LRU is the v0.10.17 D4 hygiene substrate + the v0.10.17 D5 cross-component pin substrate.

**HEAD state at this brief:** `main` is the SOLE branch (no `v0.10.18/mimo-half` worktree yet; cycle-execution lands it per the v0.10.16 SPEC pattern). The exact SHA is whatever `git rev-parse HEAD` resolves to at cycle-start time and is DELIBERATELY not pinned here — each subsequent polish/hygiene commit (drift-fix, pair-mention sweep, ledger close-out, v0.10.18 brief itself, F16 README sync) would re-stale a hard SHA citation. **The durable invariant** of the post-v0.10.17 close-out chain is: **all commits since `v0.10.17` are docs-only ledger close-outs** (F16 README sync + the v0.10.18 brief + any subsequent polish passes); the SHA family is `<v0.10.17-anchor> → <1+ F16 + brief + polish commits>`. Tags on `main`: `v0.10.13` + `v0.10.14` + `v0.10.15` + `v0.10.17` + 26 historical. **NO `v0.10.16` tag** (deferred per AUDIT-2026-07-12-d21e840.md). **NO `v0.10.17.1` tag** (the v0.10.17 NICE-TO-HAVE polish items — DRY tick(ms) helper + `GATEWAY_DEFAULT_WINDOW_S` constant + drop redundant `advance(0)` + verify-tab-toggle cache-hit — are deferred to a v0.10.18.1 explicit user approval; do NOT batch into this cycle). **Final-cycle-start health-check:** if the durable invariant ("all docs-only since v0.10.17") has changed materially (e.g. a code commit landed without this brief), this brief is STALE and must be re-baselined before MiMo credits are spent.

**MiMo execution model:**
- All work ships on a working branch `v0.10.18/mimo-half`, then **fast-forward merge** to `main` (per `CONTRIBUTING.md`'s linear-history rule). Pattern matches the v0.10.16 SPEC + v0.10.17 execution.
- **4 atomic commits** (one per deliverable, D1 → D2 → D3 → D4), each Conventional Commits format with a plan reference. No batching.
- After the 4 atomic commits, the **parent agent (Buffy)** handles close-out: merge to `main`, tag `v0.10.18`, `gh release create` publish. The D2 Playwright scope fits one half-buffy; the diagnostic-first cycle on D1 may exceed half-buffy if the root cause is multi-day (NAKE escalation in §"Failure modes" below).

---

## Anti-regression: 6 contracts from v0.10.15 + v0.10.17 cycle shipts

These contracts MUST NOT regress. Every test you write observes the post-state; every code change preserves the contract.

| Contract | File | Test-contract requirement |
|---|---|---|
| `except (ConnectionError, OSError, TimeoutError, redis.exceptions.RedisError)` (v0.10.15 plan 032 narrowing) | `apps/api/src/gw2analytics_api/main.py:113` | Tests on `arq pool init` failures MUST observe narrow-catch behavior. Other exception types propagate. |
| `except (InvalidToken, UnicodeDecodeError, SQLAlchemyError)` (v0.10.15 plan 033 narrowing) | `apps/api/scripts/rotate_kek.py:104` | Tests on per-row decrypt-fail behavior MUST observe narrow-catch behavior. |
| `subscription_id = subscription_id or None` collapse (v0.10.15 plan 034 type-convergence) | `apps/api/src/gw2analytics_api/routes/webhooks.py:294` | Tests on `/api/v1/webhooks/dlq` MUST assert `subscription_id is None` on `?subscription_id=`. |
| 4 per-section error chips (v0.10.15 plan 035 UX hardening) | `web/src/app/fights/[id]/page.tsx` | Tests on `/fights/[id]` MUST assert the 4 chips' `data-testid` appear when their respective fetcher rejects. |
| `fetchCached` LRU (8 entries, 60s TTL, dedup, no-cache-on-error) (v0.10.14 D2) | `web/src/lib/fetchCached.ts` | Tests on `/fights/[id]` fetchers MUST observe cached/dedup/cap-eviction behavior. The v0.10.17 **D4 pin test** `web/tests/lib/fetchCached-isolation.test.ts` MUST stay GREEN. |
| F18 Replay UI (v0.10.17 D1 + D2 + D5 surface) | `web/src/components/ReplayPlayer.tsx` + `web/src/lib/replayFetcher.ts` + `web/src/app/fights/[id]/page.tsx` | Tests on `/fights/[id]` MUST observe the Replay tab + the `fetchReplayTimeline` wiring. The v0.10.17 **D5 cross-component pin** `web/tests/lib/replay-substrate-integration.test.ts` MUST stay GREEN (verifies windowS=5 cache-key preservation). |
| CHANGELOG ledger bucketing pattern (v0.10.16 `d21e840 + 66ce364`) | `CHANGELOG.md` | NO REGRESSION: do not undo the bucketing move. The `[Unreleased]` body stays empty; new dated entries are added in-place. |

---

## The 4 deliverables

### D1 — Residual 6 vitest failures in `fight-events-page*.test.tsx` (O6 carry-forward)

**Source plan:** [`plans/AUDIT-2026-07-13-3b2e71f.md`](./AUDIT-2026-07-13-3b2e71f.md) §"Open findings — O6" + §"Recommended execution order" v0.10.18 item 1.

**Scope:** Land the work to close the 6 residual pre-existing vitest failures in `web/tests/components/fight-events-page*` (down from 7 pre-v0.10.17 D3, which closed `window-size-selector.test.tsx` via `vi.hoisted` fix). The DIAGNOSTIC-FIRST cycle per the AUDIT hypothesis:

**Diagnostic phase (mandatory):**
1. Capture the FULL vitest stdout/stderr for each of the 6 failing tests in isolation. Use `pnpm vitest run tests/components/fight-events-page.test.tsx 2>&1 | head -200` per file to surface the assertion library's diff output.
2. Classify each failure by ROOT CAUSE into one of these buckets:
   - **A. `window.fetch` mocking gap** — the test uses `globalThis.fetch` directly without mock, but jsdom's `fetch` is undefined or rejects. Fix: import `vi.fn()` for `globalThis.fetch` + mockResolvedValue per fixture.
   - **B. `fetchCached` LRU interaction** — the test uses `fetchCached` (transitively via the page's fetcher abstraction) and asserts a single fetch call but the LRU returns 0 round-trips for cached entries. Fix: clear the LRU cache in `beforeEach` via `__resetCacheForTests()` (the v0.10.14 D2 export).
   - **C. React 18+ auto-batching race** — the test dispatches a click + reads DOM synchronously but the React batch defers the state update. Fix: wrap fireEvent calls in `act()` or use `findBy*` queries instead of `getBy*`.
   - **D. Date / TZ shift** — the test seeds a fight at a UTC instant that doesn't match the expected local-bucketted date after the v0.8.9 TZ shift. Fix: pin `tz=UTC` explicitly OR pick a fixture instant that doesn't straddle midnight.
   - **E. Other** — anything not in A-D. Investigate individually.

3. Produce `web/tests/FAILURE_ANALYSIS.md` NEW with one section per failure: the FILE + LINE + EXPECTED vs ACTUAL quote + the bucket classification + the fix proposal + the fix's expected LoC delta.

**Fix phase (after diagnostic):**
- Land the smallest possible commit per failure (1 fix per atomic commit if feasible, OR group A/B/C bucket fixes into themed commits if 2-3 failures share a root cause).
- The diagnostic OUTPUT (= the FAILURE_ANALYSIS.md file) IS D1's primary deliverable. Without it, the fixes are blind; with it, the fixes are reviewable in isolation.

**File-level targets:**
- `web/tests/FAILURE_ANALYSIS.md` NEW: classifies the 6 residual vitest failures (F-VT-1..F-VT-6 in this cycle's numbering, NOT continuing the v0.10.16 / v0.10.17 numbering — D3 already closed F-VT-1).
- 1-3 web/tests/components/fight-events-page* test files MODIFIED (~10-50 LoC per fix; total budget ≤ 100 LoC across all 6 fixes).

**Out of scope:** D1 does NOT touch apps/api (the pytest failures are D2's scope). D1 does NOT touch `web/src/` production code unless a root cause is bucket B (`fetchCached` interaction) — and even then, the fix lives in the TEST, not in `fetchCached.ts` itself (D4 already locks `fetchCached` semantics via the v0.10.17 D4 pin test).

**D1 done criteria:**
1. `web/tests/FAILURE_ANALYSIS.md` exists with all 6 failures classified into A/B/C/D buckets.
2. `cd web && pnpm vitest run` reports **168+ pass** (post-D1 + post-D2 + post-D3: was 162 post-v0.10.17 → 6 D1 closes + ~3-4 D3 NEW = 168+).
3. **Anti-regression**: `web/tests/lib/fetchCached-isolation.test.ts` (v0.10.17 D4) stays GREEN (6/6 PASS) — a D1 fix that touches `fetchCached.ts` would invalidate the anti-regression substrate.

### D2 — Residual 2 pytest failures in `test_uploads_e2e.py` (O6 carry-forward backend-touching)

**Source plan:** [`plans/AUDIT-2026-07-13-3b2e71f.md`](./AUDIT-2026-07-13-3b2e71f.md) §"Open findings — O6" + §"Recommended execution order" v0.10.18 item 1.

**Scope:** Land the work to close the 2 residual pre-existing pytest failures in `apps/api/tests/test_uploads_e2e.py`. These have been stable since the v0.10.14 release notes (no resolution since v0.10.15 → v0.10.16 → v0.10.17; this is the FIRST backend-touching cycle since v0.10.15).

**Diagnostic phase (mandatory):**
1. Run the 2 failures in isolation: `uv run pytest apps/api/tests/test_uploads_e2e.py -v --tb=long 2>&1 | grep -E '^FAILED|^PASSED|^ERROR'` to get the test names.
2. Capture the FULL pytest failure output for each: `uv run pytest apps/api/tests/test_uploads_e2e.py::<test_name> -v --tb=long` per failing test, surface the assertion library's diff output.
3. Classify each failure by ROOT CAUSE into one of these buckets:
   - **F. Stale fixture data** — the test seeds the DB and asserts a query result, but a v0.10.0+ schema migration broke the fixture OR the assertion query. Fix: update the fixture OR the assertion query (NOT the production code if production is correct).
   - **G. Race condition** — the test polls an upload status but the race window between parse commit and so-called dispatch surface is no longer deterministic post-v0.10.1 (plan 010) due to the Arq worker. Fix: pin the test's wait time + use the new `_isolate_test_state` conftest fixtures.
   - **H. Migration drift** — the test asserts against a column or table that no longer exists post-migration. Fix: update the assertion to the new schema.
   - **I. TZ drift** — the failing test uses a UTC fixture that lands on the wrong day post-v0.8.9's `?tz=` shift. Fix: pin `tz=UTC` explicitly OR pick a non-midnight fixture instant.
   - **J. Other** — anything not in F-I.

4. Produce `apps/api/tests/FAILURE_ANALYSIS.md` NEW with one section per failure (mirroring the v0.10.17 cycle's plan for the O6 original scope, BUT only covering the 2 pytest failures; this per-cycle scoping prevents the FAILURE_ANALYSIS files from growing unbounded).

**Fix phase:**
- 1 atomic commit per failure (preferred) OR a single commit per root-cause bucket.
- The diagnostic OUTPUT (= the apps/api FAILURE_ANALYSIS.md file) IS D2's primary deliverable.
- DO NOT touch `apps/api/src/` production code unless a root cause forces it (likely not — most failures are stable test-side issues from 2026-07-09 forward).

**File-level targets:**
- `apps/api/tests/FAILURE_ANALYSIS.md` NEW: classifies the 2 pre-existing pytest failures (F-PY-1 / F-PY-2 in this cycle's numbering).
- 1-2 web/apps/api/tests/test_uploads_e2e.py MODIFIED (~10-50 LoC per fix; total budget ≤ 80 LoC).

**Out of scope:** D2 does NOT touch `apps/api/src/` for the fixes unless a root cause bucket (H migration drift) forces an `OrmUpload` schema verification check. D2 does NOT touch the apps/api alembic migrations (the migrations are immutable by CONVENTIONAL COMMITS / per CONTRIBUTING.md).

**D2 done criteria:**
1. `apps/api/tests/FAILURE_ANALYSIS.md` exists with both failures classified.
2. `uv run pytest apps/api/tests/test_uploads_e2e.py` reports all-pass (was 2 fail pre-D2).
3. **Anti-regression**: `uv run pytest apps/api/tests/test_fights_blob_cache_thundering_herd.py` stays GREEN (8/8 PASS — the v0.10.10 cold-phase flake anti-regression; a D2 fix that breaks this is a regression to the v0.10.4 cycle's surface).

### D3 — Land the deferred D2 Playwright e2e spec (`web/tests/e2e/replay-ui.spec.ts`, deferred from v0.10.17 D2)

**Source plan:** [`plans/v0.10.17-mimo-half-prompt.md`](./v0.10.17-mimo-half-prompt.md) §"D2" v0.10.17 deferred note + [`plans/RELEASE-v0.10.17.md`](./RELEASE-v0.10.17.md) "Web Playwright: unchanged" deferral acknowledgement.

**Scope:** Land the v0.10.17 D2 Playwright e2e spec that the brief scoped but the cycle deferred to a followup:

- `web/tests/e2e/replay-ui.spec.ts` NEW (~70 LoC, 3-4 cases):
  - Page tab strip shows "Replay" tab on `/fights/<real-fight-id>`.
  - Scrubber responds to mouse drag (drag the input range + assert `aria-valuenow` updates).
  - Bucket bars update at scrubber position (assert the badge text changes from `B1` to `B5` after a forward drag).
  - No console errors on the Replay route (assert `console.error` was not called during the interactions).
- Use the existing `web/tests/e2e/mock-server.mjs` fixture infrastructure (the v0.9.0 cross-account-timeline fixture + the v0.10.14 BFF stub).

**File-level targets:**
- `web/tests/e2e/replay-ui.spec.ts` NEW (~70 LoC, 3-4 cases).
- `web/tests/e2e/mock-server.mjs`: ADD (not necessarily NEW) — the new endpoint `/api/v1/fights/<id>/timeline?window_s=N` for the page's sixth `Promise.allSettled` fetch per the v0.10.17 D1 page.tsx wiring. The existing 21 e2e tests MUST still pass on this mock-server addition.

**Out of scope:** D3 does NOT touch `web/src/components/ReplayPlayer.tsx` (the v0.10.17 D1 surface is finalized + code-reviewer PASS). D3 does NOT rework the page's `Promise.allSettled` order.

**D3 done criteria:**
1. `pnpm playwright test web/tests/e2e/replay-ui.spec.ts` reports all-pass.
2. **Anti-regression**: `pnpm playwright test` reports all-pass on the cumulative spec (21 pre-existing + 3-4 D3 NEW = 24-25 total). The existing 21 cases MUST NOT regress.
3. **Cross-component**: the `fetchReplayTimeline` URL construction pattern (windowS=5 URL omits, windowS!==5 URL includes, encodeURIComponent defensiveness on fightId) is implicitly tested via the existing mock-server stubs.

### D4 — F16 README 9th-route sync (S-effort docs-only)

**Source plan:** [`plans/AUDIT-2026-07-13-3b2e71f.md`](./AUDIT-2026-07-13-3b2e71f.md) §"Findings carried forward — F16" + §"Recommended execution order" v0.10.18 item 2.

**Scope:** Update `web/README.md`'s `## API surface` table from 8 routes documented to 9. The 9th route is the v0.10.17 Replay UI's:

- Either `/fights/[id]?tab=replay` (the page route) OR
- The implicit `/api/v1/fights/{id}/timeline?window_s=N` endpoint (the `fetchReplayTimeline` wrapper's underlying call).

**File-level targets:**
- `web/README.md` MODIFIED (1-2 LoC delta — appending the 9th row to the existing `## API surface` table).

**Out of scope:** D4 does NOT touch the backend (no new route; the 9th route derives from existing surface). D4 does NOT add a new diagram (the existing route table is sufficient).

**D4 done criteria:**
1. `web/README.md` `## API surface` table has 9 rows.
2. The 9th row's label matches the v0.10.17 D1 Replay UI convention: `GET /api/v1/fights/{id}/timeline?window_s={N}` (paginated, optional window_s query).
3. **No regression**: the existing 8 rows are unchanged in labels + paths + descriptions.

---

## Validation gates (run in order, all must be ✅)

After D1 lands, before D2:

1. `git diff --name-only main..v0.10.18/mimo-half` shows ONLY the D1 files (no premature D2/D3/D4 changes).
2. `cd web && pnpm tsc --noEmit` ✅ GREEN (no `web/src/` changes from D1; no new type drift).
3. `cd web && pnpm vitest run` ✅ reports at least 168 pass (162 post-v0.10.17 + 6 D1 closes).
4. **(anti-regression) `cd web && pnpm vitest run tests/lib/fetchCached-isolation.test.ts` ✅ still 6/6 pass** (v0.10.17 D4 pin test untouched).
5. **(anti-regression) `cd web && pnpm vitest run tests/components/replay-player.test.tsx` ✅ still 13/13 pass** (v0.10.17 D2 component test untouched).

After D2 lands, before D3:

6. `uv run ruff check apps/api/src apps/api/scripts` ✅ GREEN (no production-code changes from D2; backend untouched).
7. `uv run mypy apps/api/src libs/gw2_core/src libs/gw2_analytics/src libs/gw2_evtc_parser/src` ✅ GREEN (74 source files).
8. `uv run pytest apps/api/tests/test_uploads_e2e.py` ✅ all-pass (was 2 fail pre-D2).
9. **(anti-regression) `uv run pytest apps/api/tests/test_fights_blob_cache_thundering_herd.py` ✅ 8/8 pass** (v0.10.10 cold-phase flake anti-regression).
10. **(anti-regression) `uv run pytest apps/api/tests/test_webhooks_e2e.py` ✅ all-pass** (the v0.10.1 + v0.9.1 + v0.9.2 webhook surface stays GREEN).

After D3 lands, before D4:

11. `cd web && pnpm tsc --noEmit` ✅ GREEN (no `web/src/` changes from D3; new spec file types check via vitest).
12. `cd web && pnpm playwright test web/tests/e2e/replay-ui.spec.ts` ✅ all-pass (3-4 cases).
13. **(anti-regression) `cd web && pnpm playwright test` ✅ all-pass** (21 pre-existing + 3-4 D3 NEW = 24-25 total; existing 21 cases unchanged).

After D4 lands, before close-out:

14. `cd web && pnpm tsc --noEmit` ✅ GREEN (readme-only change; no surface drift).
15. The `web/README.md` `## API surface` table has exactly 9 rows (was 8 pre-D4).
16. **Anti-regression for CHANGELOG bucketing** — `git diff main..v0.10.18/mimo-half -- CHANGELOG.md` should NOT contain any changes that undo the d21e840 + 66ce364 bucketing. New dated entries may be added per the cycle, but `[Unreleased]` stays empty (or gets new content if the cycle introduces WIP scope).

Final pre-closeout:

17. `cd web && pnpm vitest run` ✅ all-pass (162 post-v0.10.17 + 6 D1 closes + 3-4 D3 NEW = 171-172 total).
18. **Full smoke**: `uv run pytest apps/api/tests/ -q` ✅ (all + D2 fixes pass; no regressions).
19. **Defense anti-regression summary**: `git log --oneline -15 main..v0.10.18/mimo-half` MUST show exactly 4 atomic commits (D1 → D2 → D3 → D4) with conventional commit messages referencing this brief.

---

## Commit format + M-1 ACK protocol

**Commit format (per deliverable, atomic):**

```
<type>(<scope>): <cycle D-N header> (plan-NNN refs)

<body 6-10 lines: what + why + which file paths>

Co-Authored-By: MiMo <noreply@mimo.local>
```

Concrete examples for the v0.10.18 cycle:
- `test(web): classify 6 pre-existing vitest failures in fight-events-page* via FAILURE_ANALYSIS.md (plan 038 D1)`
- `feat(tests): close 6 pre-existing vitest failures in fight-events-page* via root-cause-bucketed fixes (plan 038 D1)`
- `test(api): classify 2 pre-existing pytest failures in test_uploads_e2e.py via FAILURE_ANALYSIS.md (plan 038 D2)`
- `feat(tests): close 2 pre-existing pytest failures in test_uploads_e2e.py via root-cause-bucketed fixes (plan 038 D2)`
- `test(web): Replay UI Playwright e2e spec (3-4 cases, plan 038 D3)`
- `docs(web): sync README ## API surface table to 9 routes (F16, plan 038 D4)`

(If D1's diagnostic + fix are too coupled to split into 2 commits, combine into ONE commit with a `### Diagnostic` + `### Fix` body split. The 4-atomic-commit budget is a target; the framework is "smallest surface per commit".)

**M-1 ACK protocol** — after each deliverable lands, post a 1-line ACK:

```
M-1 ACK D1: web/tests/FAILURE_ANALYSIS.md + N fight-events-page* test fixes (K files)
M-1 ACK D2: apps/api/tests/FAILURE_ANALYSIS.md + N test_uploads_e2e.py fixes (K files)
M-1 ACK D3: web/tests/e2e/replay-ui.spec.ts NEW + web/tests/e2e/mock-server.mjs MODIFIED (2 files)
M-1 ACK D4: web/README.md MODIFIED (1 file)
```

On any non-trivial scope expansion:

```
M-1 NAKE D<N>: <scope-expansion reason>
```

---

## Code-reviewer expectation

The parent spawns `code-reviewer-minimax-m3` after each atomic commit. Address every MUST-FIX verdict inline (one followup commit per MUST-FIX bundled into the same D<N>'s slack budget). NICE-TO-HAVE items may be deferred to a future cycle with a `// TODO(plan 038)` notation if the polish scope is meaningful.

Pay particular attention to the **D1 + D2 reviews**: the FAILURE_ANALYSIS.md files MUST classify each failure into the A/B/C/D (D1) + F/G/H/I (D2) buckets — the most-missed MUST-FIX in prior audit reviews is "root-cause bucket misclassification" (filing a `fetchCached` interaction as a TZ-drift issue). The FAILURE_ANALYSIS.md is the diagnostic ledger that the rest of the fixes stand on.

---

## Failure modes (NAKE escalation triggers)

- **D1 introduces >50 LoC of test changes** for a single root-cause bucket. The scope is wrong — split into 2 cycles OR de-bucket further. (Anti-regression: `fetchCached-isolation` MUST stay GREEN.)
- **D1 misclassifies a root cause** (e.g. filing a `window.fetch` mocking gap as a `fetchCached` interaction). The fix might touch the wrong layer; recommend a NAKE + re-classification pass.
- **D1 fixes regress the v0.10.17 D5 substrate pin** (`replay-substrate-integration.test.ts` 6/6 → <6 after D1). STOP immediately + report the regression. Escalate to NAKE if the regression is structural.
- **D2 introduces >40 LoC of pytest changes** for a single root-cause bucket. The scope is wrong — split OR defer.
- **D2 touches `apps/api/src/` production code** for a fix that ostensibly could be test-only. STOP + NAKE — D2 should be test-side by design.
- **D2 regresses the v0.10.10 `test_fights_blob_cache_thundering_herd.py` 8/8 anti-regression**. STOP + NAKE.
- **D3 breaks the existing 21 e2e tests** (off-repo fixture policy = `$WVW_ANALYTICS_DIR`; a new mock-server endpoint must be additive to the existing routes, not replace them). NAKE if the regression is from the mock-server change.
- **D3 Playwright spec uses a `page.route` stub that locks in a specific replayFetcher URL shape** (e.g. `\/timeline\?window_s=10$`). The v0.10.17 D1 round-2 fix made the URL conditionally include `?window_s=` so the spec MUST pattern-match the V0.10.17 D5 substrate pin's exact URL semantics. NAKE if a future URL-shape change would silently break this spec.
- **D4 breaks the existing 8 README routes documentation** by overwriting in place. The 9th route is appended (NOT inserted); the existing 8 labels unchanged.
- **Any cycle deliverable introduces a NEW pre-existing failure** (net test count decreases vs pre-D1/D2/D3/D4). STOP + NAKE.
- **D1 over-defers >3 of 6 vitest failures into bin "deferred"** — the cycle scope needs re-evaluation; the brief explicitly says "close" not "diagnose-classify-defer".

---

## Out of scope (for the v0.10.18 cycle)

- **v0.10.17.1 polish hotfix** (DRY tick(ms) helper + `GATEWAY_DEFAULT_WINDOW_S` constant + drop redundant `vi.advanceTimersByTime(0)` + verify-tab-toggle cache-hit) — DEFER to explicit user approval as a v0.10.17.1 hotfix if the maintainer requests.
- **F17 Combat readout** (XL+ effort, blocked on statechange parser + skills DB) — defer to v0.10.19+.
- **F20 ag-grid bundle tree-shake followup** — still open from v0.10.14 audit; deferred until upstream tree-shake path lands.
- **F8/F9 god-module carry-forward** — partial from v0.10.4 split; no new ship needed unless scope expands.
- **New features beyond O6 + F16** — this cycle is O6 completion + F16 sync.
- **Production-code refactors beyond the minimum needed for D1/D2 fixes** — D1 + D2 are diagnostic-fix cycles; production code stays untouched unless a root cause forces it.
- **New infra** — no docker-compose, Makefile, or Caddyfile changes.
- **Documentation beyond the FAILURE_ANALYSIS.md files, the D3 Playwright spec, and the D4 README sync** — RELEASE/RELEASE+CHANGELOG closeout is the parent's scope.

---

## Cycle-end cross-references

The parent agent (Buffy) owns:
- `CHANGELOG.md`: insert `[0.10.18] - 2026-07-20: O6 pre-existing tests fix-up + F16 README 9th-route sync (plan 038 D1-D4)` above `[Unreleased]`. **MiMo MUST NOT write to CHANGELOG.md in any of D1-D4.**
- `plans/RELEASE-v0.10.18.md` (NEW): the cycle release notes (parent writes following the v0.10.17 pattern).
- `plans/AUDIT-2026-07-20-{short-sha}.md` (NEW): the cycle-end audit (parent writes following the v0.10.17 pattern).

---

## Plan v0.10.19 scope (next cycle after v0.10.18)

Per the cycle cadence + the deferred F17/F20 backlog:
- **F17 Combat readout** (XL+ effort) — unblocked once v0.10.18 ships clean gate contract (the pre-existing test count goes from 8 → 0; the analyst has full signal on the codebase state).
- **F20 ag-grid bundle tree-shake** (M effort) — the upstream tree-shake path is the only blocker; revisit when ag-grid-community publishes a deferred-import path.
- **(optional) v0.10.17.1 polish hotfix** (S effort) — the NICE-TO-HAVE items from v0.10.17 D2 if the maintainer requests it as a separate small cycle.

The v0.10.19 brief will be written AFTER v0.10.18 closes, so the prior cycle's outcomes inform the next scope.

---

**Ready-to-execute signal:** MiMo creates branch `v0.10.18/mimo-half` from `origin/main` HEAD (execute `git rev-parse HEAD` at cycle-start to get the current SHA; the actual SHA is irrelevant for cycle-execution purposes — only that it is a doc-only post-v0.10.17 commit), then executes D1 → D2 → D3 → D4 in that order with one atomic commit per deliverable, sending `M-1 ACK D<N>: ...` per-deliverable ACK signal for parent verification.
